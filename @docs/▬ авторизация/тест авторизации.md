Манипуляции с токенами и как их использовать?

Thought for 3s

Манипуляции с токенами — это инструмент для «стресс-тестирования» логики безопасности вашего браузера. В реальной жизни токен может истечь через час или повредиться из-за программного сбоя, но разработчику некогда ждать час, чтобы проверить, сработает ли авто-обновление.

Эта секция позволяет мгновенно имитировать критические ситуации на стороне клиента. Вот как их использовать и зачем:

1. «Протушить» токен (Expire Token)
   Что делает: Она не удаляет токен, а программно ставит ему статус «Истек» в оперативной памяти приложения.
   Зачем нужно: Чтобы проверить, как поведет себя приложение при следующем запросе к API.
   Как использовать:
   Нажмите «Протушить токен». Вы увидите в логах, что статус изменился на Expired.
   Теперь перейдите во вкладку «Симулятор» или «Песочница» и попробуйте отправить любой обычный запрос (например, «Профиль»).
   Результат: Вы увидите «магию» — интерцептор заметит «протухший» статус, сам сходит за новым токеном (Refresh) и только после этого выполнит ваш запрос. Это доказывает, что механизм бесшовного продления сессии работает.
2. Испортить токен (Corrupt LocalStorage)
   Что делает: Записывает в localStorage (в ключ accessToken) случайный набор символов («мусор»), который невозможно расшифровать.
   Зачем нужно: Это имитация хакерской атаки или серьезного сбоя данных. Мы проверяем систему на «дуракоустойчивость».
   Как использовать:
   Нажмите «Испортить токен». Теперь в браузере лежит невалидная строка.
   Попробуйте обновить страницу или отправить запрос.
   Результат: Система должна понять, что токен — это мусор, и вместо бесконечных попыток обновления должна либо очистить данные, либо вежливо выбросить вас на страницу логина (Logout). Это тест на безопасность: приложение не должно «повиснуть» с битыми данными.
3. Сбросить результаты (Clear)
   Что делает: Просто очищает историю ваших тестов в окне симулятора.
   Зачем нужно: Чтобы «начать с чистого листа» после серии экспериментов и не путаться в старых сообщениях об ошибках.
   Резюме:
   Эти инструменты нужны для QA-инженеров и разработчиков, чтобы гарантировать, что пользователь никогда не увидит «белый экран» или техническую ошибку, если с его токеном что-то пойдет не так.

Совет: После нажатия «Испортить токен» вам, скорее всего, придется залогиниться заново — и это будет признаком того, что защита работает правильно! 🛡️
