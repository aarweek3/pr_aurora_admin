# Создаст папку с датой и временем

$version = Get-Date -Format "yyyy-MM-dd_HH-mm"
Copy-Item -Path "src\app\shared\components\ui\vs-modal-compromise" -Destination "backups\vs-modal-compromise_v_$version" -Recurse

$version = Get-Date -Format "yyyy-MM-dd_HH-mm"
Copy-Item -Path "src\app\shared\components\av-image-editor-vs" -Destination "backups\av-image-editor-vs_v_$version" -Recurse
