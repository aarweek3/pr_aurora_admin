# ТЗ: Плагин `av-align-image` для TinyMCE

**Дата:** 7 января 2026 г.
**Версия:** 1.0
**Статус:** В разработке

---

## 📋 Обзор

Плагин для TinyMCE, который добавляет кнопки в глобальный toolbar для выравнивания уже вставленных изображений в редакторе.

---

## 🎯 Цель

Предоставить пользователям удобный способ выравнивания изображений (left/center/right) через кнопки в основной панели инструментов, не открывая модальное окно `av-image`.

---

## 🔧 Функциональные требования

### 1. Кнопки в Toolbar

Плагин должен зарегистрировать **3 кнопки** в глобальной панели инструментов TinyMCE:

| Кнопка | ID | Иконка | Название | Описание |
|--------|-----|--------|----------|----------|
| Влево | `image-align-left` | `align-left` | Выровнять изображение влево | Выравнивает выбранное изображение по левому краю с обтеканием текста справа |
| По центру | `image-align-center` | `align-center` | Выровнять изображение по центру | Центрирует изображение без обтекания |
| Вправо | `image-align-right` | `align-right` | Выровнять изображение вправо | Выравнивает выбранное изображение по правому краю с обтеканием текста слева |

### 2. Иконки

Использовать **встроенные иконки TinyMCE**:
- `align-left` - стрелка влево
- `align-center` - стрелка по центру
- `align-right` - стрелка вправо

### 3. Логика работы

#### Активация кнопок:
- Кнопки **активны** только когда курсор находится на изображении или `<figure>` с классом `aurora-image`
- Кнопки **неактивны** во всех остальных случаях

#### Индикация активного состояния:
- Кнопка подсвечивается (класс `.tox-tbtn--enabled`), если выбранное изображение имеет соответствующее выравнивание
- Например, если изображение выровнено влево (`data-align="left"`), кнопка `image-align-left` должна быть подсвечена

#### Действие при клике:
1. Найти выбранный элемент (img или figure.aurora-image)
2. Обновить атрибут `data-align` на выбранное значение (`left`, `center`, `right`)
3. Применить соответствующие inline-стили:
   - **Left**: `float: left; margin: 0 16px 8px 0;`
   - **Center**: `margin-left: auto; margin-right: auto; display: table; float: none;`
   - **Right**: `float: right; margin: 0 0 8px 16px;`
4. Очистить предыдущие стили выравнивания

---

## 🏗️ Техническая архитектура

### Структура файлов

```
src/assets/tinymce/plugins/av-align-image/
├── plugin.js              # Основной файл плагина
└── av_align-image-plagin.md  # ТЗ (этот файл)
```

### Регистрация плагина

```javascript
tinymce.PluginManager.add('av-align-image', function(editor) {
  // Код плагина
});
```

### Регистрация кнопок

```javascript
// Кнопка "Влево"
editor.ui.registry.addButton('image-align-left', {
  icon: 'align-left',
  tooltip: 'Выровнять изображение влево',
  onAction: function() {
    alignSelectedImage('left');
  },
  onSetup: function(api) {
    return editor.selection.selectorChangedWithUnbind(
      'img, figure.aurora-image',
      function(state) {
        api.setActive(state && getCurrentAlignment() === 'left');
      }
    ).unbind;
  }
});

// Аналогично для center и right
```

### Функция выравнивания

```javascript
function alignSelectedImage(alignment) {
  const selectedElement = editor.selection.getNode();
  let target = null;

  // Определяем целевой элемент (img или figure)
  if (selectedElement.tagName === 'IMG') {
    target = selectedElement.closest('figure.aurora-image') || selectedElement;
  } else if (selectedElement.classList.contains('aurora-image')) {
    target = selectedElement;
  }

  if (!target) return;

  // Обновляем data-align
  target.setAttribute('data-align', alignment);

  // Применяем стили
  const styles = getAlignmentStyles(alignment);
  applyStyles(target, styles);

  // Уведомляем редактор об изменении
  editor.fire('change');
}
```

### Получение стилей выравнивания

```javascript
function getAlignmentStyles(alignment) {
  const styles = {
    left: {
      float: 'left',
      margin: '0 16px 8px 0',
      marginLeft: '',
      marginRight: '16px',
      display: ''
    },
    center: {
      float: 'none',
      margin: '0 auto',
      marginLeft: 'auto',
      marginRight: 'auto',
      display: 'table'
    },
    right: {
      float: 'right',
      margin: '0 0 8px 16px',
      marginLeft: '16px',
      marginRight: '',
      display: ''
    }
  };

  return styles[alignment] || styles.center;
}
```

### Проверка текущего выравнивания

```javascript
function getCurrentAlignment() {
  const selectedElement = editor.selection.getNode();
  let target = null;

  if (selectedElement.tagName === 'IMG') {
    target = selectedElement.closest('figure.aurora-image') || selectedElement;
  } else if (selectedElement.classList.contains('aurora-image')) {
    target = selectedElement;
  }

  if (!target) return null;

  return target.getAttribute('data-align') || 'center';
}
```

---

## 🎨 UI/UX требования

### 1. Расположение кнопок в Toolbar

Кнопки должны быть добавлены в секцию форматирования, рядом с другими кнопками выравнивания текста.

Пример конфигурации в `tinymce.init()`:

```javascript
toolbar: 'undo redo | formatselect | bold italic | alignleft aligncenter alignright | image-align-left image-align-center image-align-right | av-image-text'
```

### 2. Визуальная обратная связь

- ✅ Кнопка подсвечивается при наведении (hover)
- ✅ Кнопка показывает активное состояние (active) когда изображение имеет соответствующее выравнивание
- ✅ Кнопка неактивна (disabled) когда курсор не на изображении

### 3. Tooltip

При наведении на кнопку должна появляться подсказка:
- "Выровнять изображение влево"
- "Выровнять изображение по центру"
- "Выровнять изображение вправо"

---

## 🔍 Целевые элементы

Плагин работает с двумя типами элементов:

### 1. Простое изображение
```html
<img src="..." alt="..." data-align="left">
```

### 2. Изображение в контейнере Aurora
```html
<figure class="aurora-image" data-align="center" data-width="100%">
  <img class="aurora-image__img" src="..." alt="...">
  <figcaption class="aurora-image__caption">Подпись</figcaption>
</figure>
```

**Приоритет:** Если изображение внутри `<figure>`, стили применяются к `<figure>`, а не к `<img>`.

---

## 📊 Примеры использования

### Сценарий 1: Выравнивание изображения влево

**Исходный HTML:**
```html
<figure class="aurora-image" data-align="center" style="margin-left: auto; margin-right: auto; display: table;">
  <img src="image.jpg" alt="Пример">
</figure>
```

**После клика на кнопку "Влево":**
```html
<figure class="aurora-image" data-align="left" style="float: left; margin: 0 16px 8px 0;">
  <img src="image.jpg" alt="Пример">
</figure>
```

### Сценарий 2: Выравнивание изображения по центру

**Исходный HTML:**
```html
<img src="image.jpg" alt="Пример" data-align="right" style="float: right; margin: 0 0 8px 16px;">
```

**После клика на кнопку "По центру":**
```html
<img src="image.jpg" alt="Пример" data-align="center" style="float: none; margin-left: auto; margin-right: auto; display: table;">
```

---

## ⚙️ Конфигурация плагина

### Подключение плагина

В основном конфиге TinyMCE:

```javascript
tinymce.init({
  selector: 'textarea',
  plugins: 'av-image av-align-image',
  external_plugins: {
    'av-align-image': '/assets/tinymce/plugins/av-align-image/plugin.js'
  },
  toolbar: 'undo redo | formatselect | bold italic | image-align-left image-align-center image-align-right | av-image-text'
});
```

---

## ✅ Критерии приемки

- [ ] Плагин зарегистрирован и загружается без ошибок
- [ ] 3 кнопки отображаются в toolbar с правильными иконками
- [ ] Кнопки активны только когда выбрано изображение
- [ ] Клик по кнопке применяет правильное выравнивание
- [ ] Атрибут `data-align` обновляется корректно
- [ ] Inline-стили применяются правильно
- [ ] Кнопка подсвечивается когда изображение имеет соответствующее выравнивание
- [ ] Работает с простыми `<img>` и с `<figure class="aurora-image">`
- [ ] Не вызывает ошибок в консоли
- [ ] Изменения сохраняются в контенте редактора

---

## 🐛 Известные ограничения

1. **Не работает для множественного выбора**: Если выбрано несколько изображений, плагин работает только с первым
2. **Inline-стили**: Плагин использует inline-стили, что может конфликтовать с внешними CSS
3. **Совместимость**: Работает только с TinyMCE 5.x и выше

---

## 🚀 Roadmap (будущие улучшения)

- [ ] Добавить кнопку "На всю ширину" (full width)
- [ ] Поддержка множественного выбора изображений
- [ ] Интеграция с системой Undo/Redo
- [ ] Настройка отступов через UI
- [ ] Поддержка CSS-классов вместо inline-стилей
- [ ] Горячие клавиши (Ctrl+Shift+L/C/R)

---

## 📚 Связанные документы

- **Плагин av-image:** `src/assets/tinymce/plugins/av-image/plugin.js`
- **Документация TinyMCE API:** https://www.tiny.cloud/docs/
- **Icon Registry:** https://www.tiny.cloud/docs/advanced/editor-icon-identifiers/

---

## 📝 Примечания

- Плагин должен быть **легковесным** и не зависеть от внешних библиотек
- Использовать **встроенный API TinyMCE** для всех операций
- Следовать **паттернам других плагинов** в проекте
- Код должен быть **читаемым** и **хорошо документированным**

---

**Автор:** AI Assistant
**Дата создания:** 7 января 2026 г.
**Последнее обновление:** 7 января 2026 г.
