# Footnotes Plugin (Сноски)

## Описание

Плагин для создания и управления сносками в академических текстах и статьях. Поддерживает автоматическую нумерацию, навигацию туда-обратно между текстом и сносками, редактирование и удаление сносок с автоматической перенумерацией.

## Возможности

### 🔢 Создание сноски

- Выделить место в тексте
- Нажать кнопку **¹** в toolbar
- Ввести текст сноски в диалоговом окне
- Сноска автоматически нумеруется и добавляется в конец документа

### 🔄 Автоматическая нумерация

- Сноски нумеруются последовательно: ¹, ², ³...
- При удалении сноски все последующие автоматически перенумеровываются
- Номера синхронизируются между ссылками в тексте и списком сносок

### 📝 Список сносок внизу документа

- Автоматически создаётся контейнер сносок в конце документа
- Разделитель и заголовок "Сноски:"
- Каждая сноска отображается с номером и текстом
- Контейнер удаляется автоматически, когда сносок не остаётся

### 🔗 Навигация туда-обратно

**От текста к сноске:**

- Клик по номеру сноски в тексте (например, **¹**)
- Плавная прокрутка к соответствующей сноске внизу
- Подсветка сноски жёлтым цветом на 1.5 секунды

**От сноски к тексту:**

- Клик по номеру сноски в списке
- Плавная прокрутка к ссылке в тексте
- Подсветка ссылки жёлтым цветом на 1.5 секунды

### ✏️ Редактирование сноски

- Навести курсор на сноску в списке
- Появляются кнопки редактирования (✏️) и удаления (🗑️)
- Клик по ✏️ открывает диалог редактирования
- Изменения сохраняются мгновенно

### 🗑️ Удаление сноски

- Клик по кнопке 🗑️ в списке сносок
- Подтверждение удаления
- Сноска удаляется из текста и списка
- Все последующие сноски автоматически перенумеровываются

### 🎨 Стили оформления

**Ссылка в тексте:**

- Синий цвет (#4285f4)
- Жирный шрифт
- Верхний индекс (`<sup>`)
- Hover эффект с голубым фоном
- Курсор pointer

**Список сносок:**

- Контейнер с верхней границей
- Заголовок "Сноски:" (18px, жирный)
- Горизонтальный разделитель

**Элемент сноски:**

- Номер в синем круге с белым текстом
- Текст сноски серого цвета
- Кнопки редактирования/удаления появляются при hover
- Hover эффект на весь элемент (серый фон)

---

## Использование

### В интерфейсе:

**Кнопка toolbar:** `¹` (верхний индекс 1)

**Расположение:** В группе Insert (Вставка)

**Действие:**

1. Установить курсор в нужное место текста
2. Нажать кнопку **¹**
3. Ввести текст сноски в диалоговом окне
4. Нажать OK

### Программное использование:

```typescript
// Создание плагина
const footnotesPlugin = new FootnotesPlugin();

// Создать сноску
footnotesPlugin.execute(editorElement);
```

---

## Примеры использования

### Пример 1: Научная статья

**Текст:**

```
Согласно исследованию¹, эффективность метода составляет 95%.
Другие авторы² подтверждают эти данные.

─────────────────────
Сноски:
¹ Smith J. et al. (2024). "Research on Method Efficiency". Journal of Science.
² Johnson A. (2023). "Confirmation Study". Science Today.
```

### Пример 2: Академический текст

**Текст:**

```
Теория относительности¹ была сформулирована в начале XX века.
Квантовая механика² развивалась параллельно.

─────────────────────
Сноски:
¹ Альберт Эйнштейн, 1905 год
² Макс Планк, Нильс Бор, Вернер Гейзенберг
```

### Пример 3: Статья блога

**Текст:**

```
Согласно последним данным¹, рынок растёт на 15% в год.

─────────────────────
Сноски:
¹ Источник: Аналитический отчёт 2024, стр. 42
```

---

## Интеграция

### Файлы:

- **Плагин:** `src/app/editor/plugins/insert/footnotes.plugin.ts`
- **Стили:** Встроены в плагин (injectStyles)

### Регистрация в редакторе:

```typescript
// aurora-editor.component.ts
import { FootnotesPlugin } from '../plugins/insert/footnotes.plugin';

this.plugins = [
  // ...
  new FootnotesPlugin(), // Сноски (footnotes)
  // ...
];
```

### Добавление в toolbar:

```typescript
// aurora-toolbar.component.ts
get insertPlugins(): AuroraPlugin[] {
  return this.plugins.filter(
    (p) =>
      // ...
      p.name === 'footnotes' || // Сноски
      // ...
  );
}
```

---

## Технические детали

### Структура HTML

**Ссылка в тексте:**

```html
<sup
  class="aurora-footnote-ref"
  id="footnote-1234567890-1-ref"
  contenteditable="false"
  style="cursor: pointer;"
>
  1
</sup>
```

**Контейнер сносок:**

```html
<div class="aurora-footnotes-container" contenteditable="false">
  <hr class="aurora-footnotes-separator" />
  <div class="aurora-footnotes-title">Сноски:</div>

  <div class="aurora-footnote-item" id="footnote-1234567890-1">
    <span class="aurora-footnote-number">1</span>
    <span class="aurora-footnote-text">Текст сноски</span>
    <button class="aurora-footnote-btn">✏️</button>
    <button class="aurora-footnote-btn">🗑️</button>
  </div>
</div>
```

### CSS классы

```scss
.aurora-footnote-ref         // Ссылка в тексте
.aurora-footnotes-container  // Контейнер всех сносок
.aurora-footnotes-separator  // Горизонтальный разделитель
.aurora-footnotes-title      // Заголовок "Сноски:"
.aurora-footnote-item        // Элемент сноски
.aurora-footnote-number      // Номер сноски (круг)
.aurora-footnote-text        // Текст сноски
.aurora-footnote-btn         // Кнопки редактирования/удаления
```

### События

**Клик по ссылке в тексте:**

```typescript
sup.addEventListener('click', (e) => {
  e.preventDefault();
  // Прокрутка к сноске + подсветка
});
```

**Клик по номеру сноски:**

```typescript
numberSpan.addEventListener('click', (e) => {
  e.preventDefault();
  // Прокрутка к ссылке + подсветка
});
```

**Редактирование:**

```typescript
editBtn.addEventListener('click', (e) => {
  e.preventDefault();
  // Открыть prompt для редактирования
});
```

**Удаление:**

```typescript
deleteBtn.addEventListener('click', (e) => {
  e.preventDefault();
  // Подтверждение + удаление + перенумерация
});
```

---

## API

```typescript
interface FootnotesPlugin {
  name: 'footnotes';

  // Создать сноску
  execute(editorElement: HTMLElement): boolean;

  // Проверить активность
  isActive(editorElement: HTMLElement): boolean;
}

interface Footnote {
  id: string; // Уникальный ID
  number: number; // Номер сноски
  text: string; // Текст сноски
  referenceElement: HTMLElement; // Ссылка в тексте
  contentElement: HTMLElement; // Элемент в списке
}
```

---

## Особенности

- ✅ Автоматическая нумерация
- ✅ Навигация туда-обратно
- ✅ Редактирование и удаление
- ✅ Автоматическая перенумерация при удалении
- ✅ Плавная прокрутка и подсветка
- ✅ Кнопки редактирования появляются при hover
- ✅ Встроенные стили (не требуется отдельный CSS)
- ✅ `contenteditable="false"` для защиты от случайного редактирования
- ✅ Уникальные ID на основе timestamp

---

## Ограничения

⚠️ **Prompt для ввода текста**: В текущей версии используется `prompt()` для ввода и редактирования сносок. Для production рекомендуется создать модальное окно с текстовым полем.

⚠️ **Без сохранения состояния**: При перезагрузке страницы сноски сохраняются в HTML, но хранилище в WeakMap сбрасывается. Это нормально, так как HTML содержит всю информацию.

⚠️ **Один редактор**: Плагин поддерживает несколько экземпляров редактора на странице через WeakMap.

---

## Дорожная карта

### Планируется:

1. **Модальное окно вместо prompt**

   - Текстовое поле для ввода
   - Предпросмотр форматирования
   - Кнопки "Сохранить" / "Отмена"

2. **Расширенное форматирование текста сноски**

   - Поддержка жирного/курсива
   - Ссылки внутри сносок
   - Списки в сносках

3. **Экспорт сносок**

   - Копирование всех сносок в буфер обмена
   - Экспорт в формате список

4. **Настройки отображения**
   - Символы нумерации (1, 2, 3 или \*, †, ‡)
   - Позиция контейнера (внизу или сбоку)
   - Стили оформления

---

## Сравнение с аналогами

| Возможность               | Footnotes Plugin | Google Docs | Microsoft Word |
| ------------------------- | ---------------- | ----------- | -------------- |
| Автонумерация             | ✅               | ✅          | ✅             |
| Навигация туда-обратно    | ✅               | ✅          | ✅             |
| Редактирование            | ✅               | ✅          | ✅             |
| Удаление с перенумерацией | ✅               | ✅          | ✅             |
| Hover эффекты             | ✅               | ❌          | ❌             |
| Плавная прокрутка         | ✅               | ❌          | ❌             |
| Подсветка при навигации   | ✅               | ❌          | ❌             |

---

## Совместимость

- ✅ Все современные браузеры (Chrome, Firefox, Safari, Edge)
- ✅ Angular 19+
- ✅ TypeScript 5+
- ✅ Встроенные стили (не требует внешнего CSS)

---

## Примеры использования в реальной работе

### Для студентов

- **Курсовые работы:** Ссылки на источники литературы
- **Дипломы:** Академические сноски по ГОСТу
- **Рефераты:** Указание источников цитат

### Для исследователей

- **Научные статьи:** Ссылки на исследования
- **Обзоры:** Пояснения терминов
- **Диссертации:** Подробные примечания

### Для писателей

- **Романы:** Исторические комментарии
- **Статьи:** Дополнительная информация
- **Переводы:** Примечания переводчика

Footnotes Plugin - незаменимый инструмент для академического письма! 📝¹
