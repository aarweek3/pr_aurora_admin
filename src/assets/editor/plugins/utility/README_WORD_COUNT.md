# Word Count Plugin (Статистика текста)

## Описание

Плагин для подсчёта слов, символов, параграфов и времени чтения. Предназначен для писателей, редакторов и создателей контента, которым нужна детальная статистика текста.

## Возможности

### 📊 Основная статистика

#### 🔢 Подсчёт слов
- Точный подсчёт слов в тексте
- Игнорирование пустых строк и пробелов
- Поддержка всех языков

#### 📏 Подсчёт символов
- **С пробелами** - общее количество символов
- **Без пробелов** - только буквы и знаки препинания

#### 📄 Количество параграфов
- Подсчёт блочных элементов (p, div, h1-h6, li, blockquote, pre)
- Игнорирование пустых параграфов

#### 📝 Количество предложений
- Разделение по знакам препинания (. ! ?)
- Фильтрация пустых предложений

#### ⏱️ Примерное время чтения (Reading Time)
- Рассчитано на основе средней скорости чтения: **200 слов/мин**
- Формат: минуты и секунды
- Автоматическое масштабирование (сек → мин → ч)

### 📈 Дополнительная статистика

#### 📊 Средняя длина слова
- Рассчитывается как: символов без пробелов / количество слов
- Помогает оценить сложность текста

#### 📈 Средняя длина предложения
- Рассчитывается как: количество слов / количество предложений
- Индикатор читабельности текста

#### 🎯 Плотность текста
- Рассчитывается как: количество слов / количество параграфов
- Показывает насыщенность текста

---

## Использование

### В интерфейсе:

**Кнопка toolbar:** `📝` (блокнот)

**Горячая клавиша:** `Ctrl+Shift+W`

**Расположение:** В группе Utility (Утилиты)

**Действие:**
1. Нажать кнопку 📝 или `Ctrl+Shift+W`
2. Откроется модальное окно со статистикой
3. Просмотреть основную и дополнительную статистику
4. Закрыть окно кнопкой "Закрыть"

### Модальное окно

Интерфейс модального окна включает:

1. **Основная статистика** (карточки):
   - 🔢 Слов - выделено цветом (главная метрика)
   - 📏 Символов (с пробелами)
   - 📐 Символов (без пробелов)
   - 📄 Параграфов
   - 📝 Предложений
   - ⏱️ Время чтения - выделено акцентным цветом

2. **Дополнительная статистика** (список):
   - 📊 Средняя длина слова
   - 📈 Средняя длина предложения
   - 🎯 Плотность текста

3. **Информационная заметка**:
   - Пояснение о скорости чтения

---

## Программное использование

```typescript
// Создание плагина
const wordCountPlugin = new WordCountPlugin();

// Открыть модальное окно со статистикой
wordCountPlugin.execute(editorElement);

// Получить статистику программно
const stats = wordCountPlugin.calculateStatistics(editorElement);

console.log('Слов:', stats.words);
console.log('Символов:', stats.characters);
console.log('Время чтения:', stats.readingTime, 'мин');

// Получить краткую статистику для статус-бара
const statusText = wordCountPlugin.getStatusBarText(editorElement);
// Результат: "150 слов | 1024 символов"

// Форматировать время чтения
const readingTime = wordCountPlugin.formatReadingTime(2.5);
// Результат: "3 мин" (округлено)

const longReadingTime = wordCountPlugin.formatReadingTime(65);
// Результат: "1 ч 5 мин"
```

---

## Интерфейс статистики

```typescript
interface TextStatistics {
  words: number;              // Количество слов
  characters: number;         // Символы с пробелами
  charactersNoSpaces: number; // Символы без пробелов
  paragraphs: number;         // Количество параграфов
  sentences: number;          // Количество предложений
  readingTime: number;        // Время чтения в минутах
}
```

---

## Примеры

### Пример 1: Короткий текст

**Текст:**
```
Это короткий пример текста.
Всего два предложения.
```

**Статистика:**
- 🔢 Слов: **6**
- 📏 Символов (с пробелами): **53**
- 📐 Символов (без пробелов): **47**
- 📄 Параграфов: **2**
- 📝 Предложений: **2**
- ⏱️ Время чтения: **меньше минуты**

**Дополнительно:**
- 📊 Средняя длина слова: **7.8 символов**
- 📈 Средняя длина предложения: **3.0 слов**
- 🎯 Плотность текста: **3.0 слов/параграф**

---

### Пример 2: Средняя статья

**Текст:** 500 слов, 5 параграфов, 25 предложений

**Статистика:**
- 🔢 Слов: **500**
- 📏 Символов (с пробелами): **3200**
- 📐 Символов (без пробелов): **2700**
- 📄 Параграфов: **5**
- 📝 Предложений: **25**
- ⏱️ Время чтения: **3 мин**

**Дополнительно:**
- 📊 Средняя длина слова: **5.4 символов**
- 📈 Средняя длина предложения: **20.0 слов**
- 🎯 Плотность текста: **100.0 слов/параграф**

---

### Пример 3: Длинная статья

**Текст:** 3000 слов, 20 параграфов, 120 предложений

**Статистика:**
- 🔢 Слов: **3000**
- 📏 Символов (с пробелами): **19200**
- 📐 Символов (без пробелов): **16200**
- 📄 Параграфов: **20**
- 📝 Предложений: **120**
- ⏱️ Время чтения: **15 мин**

**Дополнительно:**
- 📊 Средняя длина слова: **5.4 символов**
- 📈 Средняя длина предложения: **25.0 слов**
- 🎯 Плотность текста: **150.0 слов/параграф**

---

## Интеграция

### Файлы:
- **Плагин:** `src/app/editor/plugins/utility/word-count.plugin.ts`
- **Модальное окно:** `src/app/editor/modals/word-count-modal/word-count-modal.component.ts`
- **Модальный HTML:** `src/app/editor/modals/word-count-modal/word-count-modal.component.html`
- **Модальный CSS:** `src/app/editor/modals/word-count-modal/word-count-modal.component.scss`

### Регистрация в редакторе:

```typescript
// aurora-editor.component.ts
import { WordCountPlugin } from '../plugins/utility/word-count.plugin';
import { WordCountModalComponent } from '../modals/word-count-modal/word-count-modal.component';

@Component({
  imports: [
    WordCountModalComponent, // Добавить в imports
    // ...
  ]
})
export class AuroraEditorComponent implements AfterViewInit {
  @ViewChild(WordCountModalComponent)
  wordCountModal?: WordCountModalComponent;

  this.plugins = [
    // ...
    new WordCountPlugin(), // Статистика текста (подсчёт слов)
    // ...
  ];

  ngAfterViewInit(): void {
    // Подключаем модальное окно к плагину
    const wordCountPlugin = this.plugins.find((p) => p.name === 'wordCount') as any;
    if (wordCountPlugin && this.wordCountModal) {
      wordCountPlugin.setModalComponent(this.wordCountModal);
    }
  }
}
```

### HTML шаблон:

```html
<!-- aurora-editor.component.html -->
<app-word-count-modal></app-word-count-modal>
```

### Toolbar:

```typescript
// aurora-toolbar.component.ts
get utilityPlugins(): AuroraPlugin[] {
  return this.plugins.filter(
    (p) =>
      // ...
      p.name === 'wordCount' || // Статистика текста
      // ...
  );
}
```

---

## Технические детали

### Алгоритм подсчёта слов

1. Извлечение текста через `textContent` (без HTML тегов)
2. Удаление начальных и конечных пробелов
3. Разделение по пробельным символам (`\s+`)
4. Фильтрация пустых строк
5. Подсчёт элементов массива

### Алгоритм подсчёта параграфов

1. Поиск блочных элементов: `p, div, h1-h6, li, blockquote, pre`
2. Фильтрация элементов с пустым текстом
3. Подсчёт непустых элементов
4. Минимум: 1 параграф

### Алгоритм подсчёта предложений

1. Разделение текста по знакам: `.`, `!`, `?`
2. Фильтрация пустых предложений
3. Подсчёт элементов

### Расчёт времени чтения

```typescript
const READING_SPEED_WPM = 200; // слов в минуту
const readingTime = wordCount / READING_SPEED_WPM;
```

### Форматирование времени

- `< 1 мин` → секунды (например, "45 сек")
- `1-59 мин` → минуты (например, "5 мин")
- `≥ 60 мин` → часы и минуты (например, "1 ч 30 мин")

---

## API

```typescript
interface WordCountPlugin {
  name: 'wordCount';
  shortcut: 'Ctrl+Shift+W';

  // Открыть модальное окно со статистикой
  execute(editorElement: HTMLElement): boolean;

  // Рассчитать статистику
  calculateStatistics(editorElement: HTMLElement): TextStatistics;

  // Получить краткую статистику для статус-бара
  getStatusBarText(editorElement: HTMLElement): string;

  // Форматировать время чтения
  formatReadingTime(minutes: number): string;

  // Установить модальное окно
  setModalComponent(modalComponent: any): void;
}

interface TextStatistics {
  words: number;
  characters: number;
  charactersNoSpaces: number;
  paragraphs: number;
  sentences: number;
  readingTime: number; // в минутах
}
```

---

## Использование для статус-бара

Плагин предоставляет метод для отображения краткой статистики в статус-баре редактора:

```typescript
// Получить краткую статистику
const wordCountPlugin = this.plugins.find(p => p.name === 'wordCount');
const statusText = wordCountPlugin.getStatusBarText(editorElement);

// Отобразить в статус-баре
this.statusBarText = statusText; // "150 слов | 1024 символов"
```

**Формат:** `{words} слов | {characters} символов`

---

## Производительность

- ✅ Быстрый подсчёт даже для больших документов (10000+ слов)
- ✅ Оптимизированные RegExp для разделения текста
- ✅ Минимальная обработка DOM (только textContent)
- ✅ Кэширование результатов в модальном окне

---

## Особенности

- ✅ Детальная статистика (6 основных метрик)
- ✅ Дополнительная аналитика (средние значения)
- ✅ Красивое модальное окно с градиентами
- ✅ Адаптивный дизайн
- ✅ Информационные подсказки
- ✅ Горячая клавиша для быстрого доступа
- ✅ Поддержка всех языков
- ✅ Fallback на alert если модальное окно недоступно

---

## Сравнение с аналогами

| Функция | Word Count Plugin | Google Docs | Microsoft Word |
|---------|-------------------|-------------|----------------|
| Подсчёт слов | ✅ | ✅ | ✅ |
| Символы с/без пробелов | ✅ | ✅ | ✅ |
| Время чтения | ✅ | ❌ | ❌ |
| Средняя длина слова | ✅ | ❌ | ❌ |
| Средняя длина предложения | ✅ | ❌ | ❌ |
| Плотность текста | ✅ | ❌ | ❌ |
| Горячая клавиша | ✅ | ✅ | ✅ |

---

## Совместимость

- ✅ Все современные браузеры (Chrome, Firefox, Safari, Edge)
- ✅ Angular 19+
- ✅ TypeScript 5+
- ✅ Поддержка всех языков (Unicode)

---

## Дорожная карта

### Планируется:

1. **Дополнительные метрики**
   - Уникальные слова
   - Самые частые слова
   - Индекс читабельности (Flesch-Kincaid)

2. **Экспорт статистики**
   - Копирование в буфер обмена
   - Экспорт в CSV/JSON
   - Печать статистики

3. **Настройки**
   - Изменение скорости чтения
   - Выбор языка для подсчёта
   - Игнорирование определённых элементов

4. **Графики**
   - Визуализация статистики
   - Диаграммы распределения
   - История изменений

---

## Примеры использования в реальной работе

### Для писателей

- **Контроль объёма:** Отслеживание прогресса написания книги
- **Планирование:** Оценка времени чтения главы
- **Читабельность:** Анализ длины предложений

### Для блогеров

- **SEO оптимизация:** Контроль длины статьи (рекомендуется 1500+ слов)
- **Время чтения:** Указание reading time для читателей
- **Структура:** Анализ плотности параграфов

### Для редакторов

- **Проверка объёма:** Соответствие требованиям издателя
- **Анализ текста:** Оценка сложности по длине слов
- **Структурирование:** Контроль количества параграфов

Word Count Plugin - незаменимый инструмент для всех, кто работает с текстом! 📝
