# ТЗ: Плагин Circle для Aurora Editor

## 🎯 Описание

Плагин Circle реализует функциональность обрезки изображений в форме круга с возможностью настройки размера и положения круглой области.

## 📋 Основные требования

### 🔧 Функциональность

1. **Круговая обрезка**: Возможность обрезать изображение по кругу
2. **Интерактивный редактор**: Перетаскивание и изменение размера круглой области
3. **Предварительный просмотр**: Визуализация результата в реальном времени
4. **Сохранение прозрачности**: Область вне круга становится прозрачной (PNG)
5. **Интеграция с toolbar**: Кнопка активации в панели инструментов

### 🎨 UI/UX

- **Кнопка в toolbar**: Иконка "🔴" с подписью "Circle"
- **Интерактивная область**: Круг с границами для изменения размера
- **Центрирование**: Автоматическое центрирование круга при активации
- **Контролы**: Кнопки "Применить", "Сбросить", "Отменить"

### 🛠 Технические требования

#### Архитектура

```
src/app/editor/plugins/circle/
├── TZ_CIRCLE_PLUGIN.md          # Техническое задание
├── circle.plugin.ts             # Основной плагин
├── circle.service.ts            # Сервис обработки
├── circle.types.ts              # TypeScript интерфейсы
├── components/
│   └── circle-toolbar.component.ts  # UI компонент
└── utils/
    └── circle.utils.ts          # Вспомогательные функции
```

#### Интерфейсы

```typescript
interface CircleConfig {
  centerX: number; // Центр X (в пикселях)
  centerY: number; // Центр Y (в пикселях)
  radius: number; // Радиус круга (в пикселях)
  strokeWidth: number; // Толщина обводки
  strokeColor: string; // Цвет обводки
}

interface CircleResult {
  imageData: string; // Base64 результат
  config: CircleConfig; // Настройки обрезки
  originalSize: { width: number; height: number };
  croppedSize: { width: number; height: number };
}
```

## 🔄 Алгоритм работы

### 1. Активация плагина

```typescript
// При клике на кнопку Circle в toolbar
activateCircleTool() {
  - Переключить в режим обрезки круга
  - Показать интерактивную область
  - Рассчитать оптимальный размер круга
  - Центрировать круг на изображении
}
```

### 2. Интерактивное редактирование

```typescript
// Пользователь может:
- Перетащить центр круга (mouse drag)
- Изменить радиус (wheel/resize handles)
- Видеть превью в реальном времени
- Видеть размеры и координаты
```

### 3. Применение обрезки

```typescript
applyCircleCrop(config: CircleConfig) {
  1. Создать новый canvas размером (radius*2, radius*2)
  2. Создать круговую маску
  3. Применить маску к изображению
  4. Экспортировать как PNG с прозрачностью
  5. Обновить основной canvas
}
```

## 🎨 Canvas операции

### Создание круговой маски

```typescript
function createCircularMask(canvas: HTMLCanvasElement, config: CircleConfig) {
  const ctx = canvas.getContext('2d')!;

  // Очистить canvas
  ctx.clearRect(0, 0, canvas.width, canvas.height);

  // Создать круговой путь
  ctx.beginPath();
  ctx.arc(config.centerX, config.centerY, config.radius, 0, Math.PI * 2);
  ctx.closePath();

  // Установить режим обрезки
  ctx.clip();
}
```

### Применение к изображению

```typescript
function applyCircularCrop(image: HTMLImageElement, config: CircleConfig): string {
  const canvas = document.createElement('canvas');
  const ctx = canvas.getContext('2d')!;

  // Размер результирующего canvas = диаметр круга
  const diameter = config.radius * 2;
  canvas.width = diameter;
  canvas.height = diameter;

  // Создать круговую маску
  ctx.beginPath();
  ctx.arc(config.radius, config.radius, config.radius, 0, Math.PI * 2);
  ctx.closePath();
  ctx.clip();

  // Нарисовать изображение с учетом смещения
  const offsetX = config.radius - config.centerX;
  const offsetY = config.radius - config.centerY;
  ctx.drawImage(image, offsetX, offsetY);

  return canvas.toDataURL('image/png'); // PNG для прозрачности
}
```

## 🔌 Интеграция с системой

### Toolbar кнопка

```typescript
// В toolbar configuration
{
  id: 'circle',
  label: 'Circle',
  icon: '🔴',
  action: () => this.imageModal.activateCircleTool(),
  category: 'editing',
  shortcut: 'Ctrl+Shift+C'
}
```

### Plugin регистрация

```typescript
// В aurora-plugin.interface.ts
export interface AuroraPlugin {
  // ... существующие поля
  circleTools?: {
    activate: () => void;
    deactivate: () => void;
    apply: (config: CircleConfig) => Promise<CircleResult>;
    reset: () => void;
  };
}
```

## 📱 UI компоненты

### CircleToolbarComponent

```html
<div class="circle-toolbar" *ngIf="isActive">
  <!-- Информация о размерах -->
  <div class="circle-info">
    <span>Центр: {{centerX}}, {{centerY}}</span>
    <span>Радиус: {{radius}}px</span>
    <span>Диаметр: {{diameter}}px</span>
  </div>

  <!-- Контролы -->
  <div class="circle-controls">
    <button (click)="centerCircle()">📍 Центрировать</button>
    <button (click)="fitToImage()">📐 По размеру</button>
    <button (click)="resetCircle()">🔄 Сбросить</button>
    <button (click)="applyCircle()" class="primary">✅ Применить</button>
    <button (click)="cancelCircle()">❌ Отменить</button>
  </div>
</div>
```

### Интерактивная область

```html
<!-- Canvas overlay для отображения круга -->
<canvas
  #circleOverlay
  class="circle-overlay"
  [width]="canvasWidth"
  [height]="canvasHeight"
  (mousedown)="onMouseDown($event)"
  (mousemove)="onMouseMove($event)"
  (mouseup)="onMouseUp($event)"
  (wheel)="onWheel($event)"
>
</canvas>
```

## 🎯 Математические расчеты

### Оптимальный размер круга

```typescript
function calculateOptimalRadius(imageWidth: number, imageHeight: number): number {
  // Радиус = 80% от меньшей стороны
  return Math.min(imageWidth, imageHeight) * 0.4;
}
```

### Проверка границ

```typescript
function validateCircleBounds(
  config: CircleConfig,
  imageSize: { width: number; height: number },
): CircleConfig {
  const maxRadius = Math.min(
    config.centerX,
    config.centerY,
    imageSize.width - config.centerX,
    imageSize.height - config.centerY,
  );

  return {
    ...config,
    radius: Math.min(config.radius, maxRadius),
  };
}
```

## 🔧 Настройки и опции

### Пользовательские настройки

```typescript
interface CircleSettings {
  defaultRadius: number; // Радиус по умолчанию (% от размера)
  strokeWidth: number; // Толщина границы круга
  strokeColor: string; // Цвет границы
  fillOpacity: number; // Прозрачность заливки
  snapToCenter: boolean; // Привязка к центру
  preserveAspectRatio: boolean; // Сохранение пропорций
}
```

## 🧪 Тестирование

### Тест-кейсы

1. **Активация плагина**: Кнопка Circle активирует режим обрезки
2. **Интерактивность**: Перетаскивание и изменение размера работает
3. **Границы**: Круг не выходит за границы изображения
4. **Применение**: Создается PNG с прозрачным фоном
5. **Отмена**: Можно отменить операцию без изменений
6. **Производительность**: Работает плавно на изображениях до 4K

### Краевые случаи

- Очень маленькие изображения (< 100px)
- Очень большие изображения (> 4K)
- Квадратные vs прямоугольные изображения
- Изображения с прозрачностью

## 🚀 План реализации

### Фаза 1: Базовая функциональность

- [ ] Создать CircleService с базовыми методами
- [ ] Интегрировать кнопку в toolbar
- [ ] Реализовать активацию/деактивацию режима
- [ ] Создать простую круговую обрезку

### Фаза 2: Интерактивность

- [ ] Добавить интерактивное перетаскивание
- [ ] Реализовать изменение размера
- [ ] Добавить превью в реальном времени
- [ ] Создать UI контролы

### Фаза 3: Оптимизация

- [ ] Оптимизировать производительность
- [ ] Добавить настройки и опции
- [ ] Улучшить UX/UI
- [ ] Добавить тесты

### Фаза 4: Интеграция

- [ ] Интегрировать с системой истории
- [ ] Добавить поддержку горячих клавиш
- [ ] Оптимизировать для мобильных устройств
- [ ] Финальное тестирование

## 📦 Зависимости

### Новые пакеты

Не требуются - используем нативные Canvas API

### Существующие сервисы

- `ImageModalService` - для работы с изображениями
- `HistoryService` - для отмены/повтора операций
- Toolbar API - для регистрации кнопки

## 🎨 Стили

### CSS классы

```scss
.circle-toolbar {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 8px 16px;
  background: #f5f5f5;
  border-radius: 6px;
}

.circle-overlay {
  position: absolute;
  top: 0;
  left: 0;
  pointer-events: auto;
  cursor: move;

  &.resizing {
    cursor: nw-resize;
  }
}

.circle-info {
  font-size: 12px;
  color: #666;

  span {
    margin-right: 12px;
  }
}

.circle-controls {
  display: flex;
  gap: 8px;

  button.primary {
    background: #007bff;
    color: white;
  }
}
```

## 🔍 Техническая спецификация

### Производительность

- **Отзывчивость**: < 16мс на обновление превью
- **Память**: < 10MB дополнительного потребления
- **Совместимость**: Chrome 90+, Firefox 88+, Safari 14+

### Доступность

- Поддержка клавиатуры (Tab, Enter, Esc)
- Описания для скринридеров
- Высокий контраст интерфейса

## 📋 Критерии готовности

### Definition of Done

- [x] ТЗ создано и обсуждено
- [ ] Код реализован согласно архитектуре
- [ ] UI соответствует макетам
- [ ] Все тесты проходят
- [ ] Документация обновлена
- [ ] Code review пройден
- [ ] Performance тестирование завершено

---

**Автор**: AI Assistant
**Дата создания**: 16 декабря 2025
**Статус**: В разработке
**Приоритет**: Высокий
