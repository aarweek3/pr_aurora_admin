# CreateQuoteStyle Plugin - Плагин создания стилей цитат

## 📋 Обзор

**CreateQuoteStylePlugin** — плагин для Aurora Editor, который открывает визуальный редактор стилей цитат напрямую из тулбара, позволяя создавать кастомные стили без необходимости вставлять цитату.

## ✨ Возможности

- 🎨 **Отдельная кнопка** в тулбаре редактора
- ⌨️ **Горячая клавиша** `Ctrl+Shift+S`
- 🔄 **Переиспользует** BlockquoteStyleEditor
- 💾 **Автоматическое сохранение** в IndexedDB
- 🔗 **Интеграция** с Quote плагином
- 👁️ **Live Preview** в реальном времени

## 🚀 Использование

### Через тулбар

1. Найдите кнопку 🎨 (палитра) в тулбаре
2. Кликните на неё
3. Откроется редактор стилей цитат
4. Настройте внешний вид цитаты
5. Нажмите "Сохранить"

### Через горячую клавишу

Нажмите `Ctrl+Shift+S` (или `Cmd+Shift+S` на Mac)

## 🎯 Структура плагина

```
src/app/editor/plugins/create-quote-style/
├── create-quote-style.plugin.ts      # Главный класс плагина (~210 строк)
└── create-quote-style.config.ts      # Конфигурация (~75 строк)
```

## 🔧 Интеграция

### Подключение в редакторе

```typescript
// aurora-editor.component.ts
import { CreateQuoteStylePlugin } from '../plugins/create-quote-style/create-quote-style.plugin';

// Создание экземпляра
const createQuoteStylePlugin = new CreateQuoteStylePlugin();

// Добавление в массив плагинов
this.plugins = [
  // ... другие плагины
  quotePlugin,
  createQuoteStylePlugin, // 🎨 После Quote плагина
  // ...
];
```

### Добавление в тулбар

```typescript
// aurora-toolbar.component.ts
get formatPlugins(): AuroraPlugin[] {
  return this.plugins.filter(
    (p) =>
      // ... другие плагины
      p.name === 'quote' ||
      p.name === 'createQuoteStyle', // 🎨 Кнопка создания стилей
  );
}
```

### Горячая клавиша

```typescript
// aurora-editor.component.ts
if ((event.ctrlKey || event.metaKey) && event.shiftKey && event.key === 'S') {
  event.preventDefault();
  const plugin = this.plugins.find((p) => p.name === 'createQuoteStyle');
  if (plugin) {
    plugin.execute(this.editorElementRef.nativeElement);
  }
}
```

## 📊 API

### Конфигурация

```typescript
interface CreateQuoteStylePluginConfig {
  id: string; // 'createQuoteStyle'
  name: string; // 'createQuoteStyle'
  description: string;
  icon: string; // SVG палитры
  hotkey: string; // 'Ctrl+Shift+S'
  showInToolbar: boolean; // true
  toolbarOrder: number; // 51 (после Quote)
  debug: boolean; // false
}
```

### Методы

#### `init(): void`

Инициализация плагина. Вызывается автоматически при регистрации.

#### `execute(editorElement: HTMLElement, options?: any): boolean`

Открывает редактор стилей цитат.

**Параметры:**

- `editorElement` - элемент редактора (не используется)
- `options` - дополнительные опции (не используется)

**Возвращает:** `true` если редактор открыт успешно

#### `isActive(editorElement: HTMLElement): boolean`

Проверяет активность плагина. Всегда возвращает `false` (плагин не имеет активного состояния).

#### `destroy(): void`

Очистка ресурсов. Закрывает открытый редактор.

## 🎨 Иконка

SVG иконка палитры художника:

```svg
<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
  <path d="M12 2a10 10 0 0 1 10 10 4 4 0 0 1-4 4h-1.5a2 2 0 0 0-1.5 3.5A10 10 0 1 1 12 2z"/>
  <circle cx="7.5" cy="10.5" r="1.5"/>
  <circle cx="12" cy="7.5" r="1.5"/>
  <circle cx="16.5" cy="10.5" r="1.5"/>
</svg>
```

## 🔗 Связь с Quote плагином

### Общие компоненты

| Компонент                 | Использование    |
| ------------------------- | ---------------- |
| `BlockquoteStyleEditor`   | Переиспользуется |
| `BlockquoteStylesService` | Общий сервис     |
| `IndexedDB (AuroraDB)`    | Общее хранилище  |

### Синхронизация

```typescript
// CreateQuoteStyle создаёт стиль
await stylesService.createCustomStyle(newStyle);

// Quote плагин видит новый стиль мгновенно (через Observable)
stylesService.getAllStyles().subscribe((styles) => {
  this.customStyles = styles.filter((s) => s.isCustom);
});
```

## ✅ Критерии приёмки

- [x] Кнопка видна в тулбаре после Quote плагина
- [x] Иконка отображается корректно (🎨 палитра)
- [x] Tooltip показывает "Создать стиль цитаты (Ctrl+Shift+S)"
- [x] При клике открывается BlockquoteStyleEditor
- [x] Редактор открывается в режиме создания (`openNew()`)
- [x] После сохранения стиль добавляется в IndexedDB
- [x] Новый стиль доступен в Quote плагине
- [x] Горячая клавиша `Ctrl+Shift+S` работает
- [x] При отмене редактор закрывается
- [x] Можно создать несколько стилей подряд
- [x] TypeScript компилируется без ошибок

## 📦 Статистика

- **Файлов:** 2
- **Строк кода:** ~285
- **Зависимости:** BlockquoteStyleEditor, BlockquoteStylesService
- **Время разработки:** ~2 часа

## 🎉 Результат

Плагин полностью реализован и интегрирован в Aurora Editor!

**Теперь доступно:**

- ✅ Кнопка 🎨 в тулбаре
- ✅ Горячая клавиша `Ctrl+Shift+S`
- ✅ Визуальный редактор стилей
- ✅ Автоматическая синхронизация с Quote плагином
