# Text Alignment Advanced Plugin (Расширенное выравнивание текста)

## Описание

Плагин расширенных настроек выравнивания и форматирования текста. Предоставляет 4 группы опций для тонкой настройки отображения текста.

## Возможности

### 📐 Text Indent (Красная строка)

Устанавливает отступ первой строки параграфа. Применяется к блочным элементам.

**Опции:**
- ⬜ **Нет отступа** - `0`
- ▫️ **1em** - малый отступ
- ▫️▫️ **2em** - средний отступ (стандарт)
- ▫️▫️▫️ **3em** - большой отступ

**CSS свойство:** `text-indent`

**Пример:**
```html
<p style="text-indent: 2em">
  Первая строка этого параграфа будет иметь отступ 2em,
  что создаёт эффект "красной строки".
</p>
```

---

### 📏 Letter Spacing (Межбуквенный интервал)

Управляет расстоянием между символами. Применяется к выделенному тексту (оборачивается в `<span>`).

**Опции:**
- 🔤 **Сжатый (-1px)** - буквы ближе друг к другу
- 🔡 **Нормальный** - стандартное расстояние
- 🔠 **Расширенный 1px** - небольшое увеличение
- 🅰️ **Расширенный 2px** - среднее увеличение
- 🅱️ **Расширенный 3px** - большое увеличение

**CSS свойство:** `letter-spacing`

**Пример:**
```html
<!-- Сжатый -->
<span style="letter-spacing: -1px">Плотный текст</span>

<!-- Расширенный -->
<span style="letter-spacing: 2px">Р а з р е ж е н н ы й   т е к с т</span>
```

---

### 📐 Word Spacing (Интервал между словами)

Управляет расстоянием между словами. Применяется к выделенному тексту (оборачивается в `<span>`).

**Опции:**
- 📉 **Сжатый (-1px)** - слова ближе
- 📊 **Нормальный** - стандартное расстояние
- 📈 **Расширенный 2px** - среднее увеличение
- 📶 **Расширенный 4px** - большое увеличение

**CSS свойство:** `word-spacing`

**Пример:**
```html
<span style="word-spacing: 4px">
  Слова в этом тексте расположены далеко друг от друга
</span>
```

---

### ↕️ Vertical Align (Вертикальное выравнивание)

Выравнивание текста по вертикали относительно базовой линии. Применяется только к inline элементам (оборачивается в `<span>`).

**Опции:**
- ⬆️ **Top (Верх)** - по верхней границе
- ↔️ **Middle (Центр)** - по центру
- ⬇️ **Bottom (Низ)** - по нижней границе
- 📏 **Baseline (Базовая линия)** - стандартное выравнивание

**CSS свойство:** `vertical-align`

**Пример:**
```html
<p>
  Обычный текст
  <span style="vertical-align: super">надстрочный</span>
  и
  <span style="vertical-align: sub">подстрочный</span>
</p>
```

---

## Использование

### В интерфейсе:

**Кнопка toolbar:** `📏` (иконка линейки)

**Тип:** Dropdown с 4 секциями

**Расположение:** В группе форматирования, после clearFormatting

### Программное использование:

```typescript
// Создание плагина
const advancedPlugin = new TextAlignmentAdvancedPlugin();

// Text Indent (красная строка)
advancedPlugin.execute(editorElement, {
  type: 'textIndent',
  value: '2em'
});

// Letter Spacing (межбуквенный интервал)
advancedPlugin.execute(editorElement, {
  type: 'letterSpacing',
  value: '2px'
});

// Word Spacing (интервал между словами)
advancedPlugin.execute(editorElement, {
  type: 'wordSpacing',
  value: '4px'
});

// Vertical Align (вертикальное выравнивание)
advancedPlugin.execute(editorElement, {
  type: 'verticalAlign',
  value: 'middle'
});
```

---

## Поведение

### Text Indent:
- Применяется к **блочным элементам** (p, div, h1-h6, blockquote, ul, ol, li)
- Если курсор в блоке, стиль применяется ко всему блоку
- Значение `0` или `normal` удаляет стиль

### Letter Spacing, Word Spacing, Vertical Align:
- Применяются к **выделенному тексту**
- Если ничего не выделено, ничего не происходит
- Текст оборачивается в `<span>` с соответствующим стилем
- Значение `normal` удаляет стиль

---

## Интеграция

### Файлы:
- **Плагин:** `src/app/editor/plugins/format/text-alignment-advanced.plugin.ts`
- **Регистрация:** `src/app/editor/components/aurora-editor.component.ts`
- **Toolbar:** `src/app/editor/components/toolbar/aurora-toolbar.component.ts`
- **Toolbar HTML:** `src/app/editor/components/toolbar/aurora-toolbar.component.html`
- **Toolbar CSS:** `src/app/editor/components/toolbar/aurora-toolbar.component.scss`

### Регистрация в редакторе:

```typescript
// aurora-editor.component.ts
import { TextAlignmentAdvancedPlugin } from '../plugins/format/text-alignment-advanced.plugin';

this.plugins = [
  // ...
  new TextAlignmentAdvancedPlugin(), // Расширенное выравнивание текста
  // ...
];
```

### Добавление в toolbar:

```typescript
// aurora-toolbar.component.ts
get formatPlugins(): AuroraPlugin[] {
  return this.plugins.filter(
    (p) =>
      // ...
      p.name === 'textAlignmentAdvanced' || // Расширенное выравнивание
      // ...
  );
}
```

---

## Примеры использования

### Пример 1: Красная строка для всех параграфов

**Применение:**
1. Установить курсор в параграф
2. Открыть dropdown 📏
3. Выбрать "📐 Красная строка (Text Indent)" → "2em"

**Результат:**
```html
<p style="text-indent: 2em">
  Текст с красной строкой...
</p>
```

---

### Пример 2: Разреженный текст (letter-spacing)

**Применение:**
1. Выделить текст
2. Открыть dropdown 📏
3. Выбрать "📏 Межбуквенный интервал" → "Расширенный 2px"

**Результат:**
```html
<span style="letter-spacing: 2px">Р а з р е ж е н н ы й   т е к с т</span>
```

---

### Пример 3: Широкие пробелы между словами

**Применение:**
1. Выделить текст
2. Открыть dropdown 📏
3. Выбрать "📐 Интервал между словами" → "Расширенный 4px"

**Результат:**
```html
<span style="word-spacing: 4px">Текст   с   широкими   пробелами</span>
```

---

### Пример 4: Вертикальное выравнивание

**Применение:**
1. Выделить текст
2. Открыть dropdown 📏
3. Выбрать "↕️ Вертикальное выравнивание" → "Top"

**Результат:**
```html
<span style="vertical-align: top">Текст выровнен по верху</span>
```

---

## Технические детали

### Dropdown структура:

Dropdown содержит 4 секции:
1. **Text Indent** (4 опции) - блочные элементы
2. **Letter Spacing** (5 опций) - inline элементы
3. **Word Spacing** (4 опции) - inline элементы
4. **Vertical Align** (4 опции) - inline элементы

Каждая секция имеет:
- **Заголовок** (`.aurora-toolbar-dropdown-section-title`) - серый фон, bold текст
- **Опции** (`.aurora-toolbar-dropdown-item`) - hover эффект

### CSS классы:

```scss
.aurora-toolbar-dropdown-section {
  border-bottom: 1px solid #e0e0e0;

  &:last-child {
    border-bottom: none;
  }
}

.aurora-toolbar-dropdown-section-title {
  padding: 8px 16px;
  font-weight: 600;
  font-size: 12px;
  color: #666;
  background-color: #f5f5f5;
  text-transform: uppercase;
}
```

---

## API

```typescript
interface TextAlignmentAdvancedPlugin {
  // Имя плагина
  name: 'textAlignmentAdvanced';

  // Выполнить команду
  execute(
    editorElement: HTMLElement,
    options?: {
      type?: 'textIndent' | 'letterSpacing' | 'wordSpacing' | 'verticalAlign';
      value?: string;
    }
  ): boolean;

  // Получить опции
  getTextIndentOptions(): TextIndentOption[];
  getLetterSpacingOptions(): LetterSpacingOption[];
  getWordSpacingOptions(): WordSpacingOption[];
  getVerticalAlignOptions(): VerticalAlignOption[];
}

interface TextIndentOption {
  name: string;
  value: string;
  emoji: string;
}

// Аналогично для других опций
```

---

## Особенности

- ✅ 4 группы настроек в одном dropdown
- ✅ Text Indent для блочных элементов
- ✅ Letter/Word Spacing для inline элементов
- ✅ Vertical Align для выделенного текста
- ✅ Визуальные эмодзи для каждой опции
- ✅ Секции с заголовками для удобной навигации
- ✅ Интегрирован с системой undo/redo

---

## Совместимость

- ✅ Все современные браузеры (Chrome, Firefox, Safari, Edge)
- ✅ Angular 19+
- ✅ TypeScript 5+

---

## Отличие от базовых плагинов выравнивания

| Плагин | Функция | CSS свойство |
|--------|---------|--------------|
| AlignLeftPlugin | Выравнивание по левому краю | `text-align: left` |
| AlignCenterPlugin | Выравнивание по центру | `text-align: center` |
| AlignRightPlugin | Выравнивание по правому краю | `text-align: right` |
| AlignJustifyPlugin | Выравнивание по ширине | `text-align: justify` |
| **TextAlignmentAdvancedPlugin** | **Тонкие настройки** | `text-indent`, `letter-spacing`, `word-spacing`, `vertical-align` |

**TextAlignmentAdvancedPlugin** дополняет базовые плагины выравнивания, предоставляя более детальный контроль над отображением текста.
