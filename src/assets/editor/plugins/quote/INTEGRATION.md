# 🔌 Интеграция Quote Plugin в Aurora Editor

## 📋 Обзор

Данный документ объясняет, как плагин **QuotePlugin** интегрирован в редактор Aurora и как он взаимодействует с UI-компонентами.

---

## 🏗️ Архитектура интеграции

### 1. Регистрация плагина в редакторе

**Файл:** `aurora-editor.component.ts`

```typescript
// Импорт плагина
import { QuotePlugin } from '../plugins/quote/quote.plugin';

// Создание экземпляра в конструкторе
constructor(...) {
  // Создаем QuotePlugin (новая версия с расширенным функционалом)
  const quotePlugin = new QuotePlugin();

  // Добавляем в массив плагинов
  this.plugins = [
    new BoldPlugin(),
    new ItalicPlugin(),
    // ... другие плагины
    quotePlugin, // ✅ Плагин цитат
    // ...
  ];
}
```

**Что происходит:**

- При инициализации редактора создаётся экземпляр `QuotePlugin`
- Плагин добавляется в массив `plugins`, который передаётся в тулбар
- Редактор автоматически вызывает `init()` для каждого плагина при `ngOnInit()`

---

### 2. Отображение кнопки в тулбаре

**Файл:** `aurora-toolbar.component.ts`

```typescript
// Плагин добавлен в категорию "Форматирование"
get formatPlugins(): AuroraPlugin[] {
  return this.plugins.filter(
    (p) =>
      p.name === 'bold' ||
      p.name === 'italic' ||
      // ...
      p.name === 'quote', // ✅ Кнопка Quote
  );
}
```

**HTML шаблон тулбара:**

```html
<div class="format-group">
  <button
    *ngFor="let plugin of formatPlugins"
    [title]="getTooltip(plugin)"
    [class.active]="isButtonActive(plugin.name)"
    (click)="onButtonClick(plugin, $event)"
  >
    <span [innerHTML]="plugin.icon"></span>
  </button>
</div>
```

**Что происходит:**

- Плагин с `name === 'quote'` попадает в категорию `formatPlugins`
- Для него создаётся кнопка с иконкой 💬
- Tooltip показывает: "Цитата (Ctrl+Shift+Q)"
- При клике вызывается `onButtonClick(plugin, event)`

---

### 3. Обработка клика по кнопке

**Тулбар эмитит событие:**

```typescript
// aurora-toolbar.component.ts
onButtonClick(plugin: AuroraPlugin, event: Event): void {
  event.preventDefault();
  this.buttonClick.emit(plugin); // ✅ Передаём плагин в редактор
}
```

**Редактор обрабатывает событие:**

```typescript
// aurora-editor.component.html
<aurora-toolbar
  [plugins]="plugins"
  [activePlugins]="activePlugins()"
  (buttonClick)="onToolbarButtonClick($event)"
></aurora-toolbar>
```

```typescript
// aurora-editor.component.ts
onToolbarButtonClick(plugin: AuroraPlugin): void {
  const executed = plugin.execute(this.editorElementRef.nativeElement);
  if (executed) {
    this.updateActivePlugins();
  }
}
```

**Что происходит:**

1. Пользователь кликает на кнопку 💬
2. Тулбар эмитит событие с объектом `quotePlugin`
3. Редактор вызывает `quotePlugin.execute(editorElement)`
4. Открывается модальное окно для создания цитаты

---

### 4. Горячая клавиша (Ctrl+Shift+Q)

**Файл:** `aurora-editor.component.ts`

```typescript
onKeyDown(event: KeyboardEvent): void {
  // ... другие горячие клавиши

  // Ctrl/Cmd + Shift + Q (Insert Quote)
  if ((event.ctrlKey || event.metaKey) && event.shiftKey && event.key === 'Q') {
    event.preventDefault();
    console.log('[AuroraEditor] Insert quote shortcut triggered');

    const quotePlugin = this.plugins.find((p) => p.name === 'quote');
    if (quotePlugin) {
      quotePlugin.execute(this.editorElementRef.nativeElement);
    }
    return;
  }

  // ...
}
```

**Что происходит:**

1. Пользователь нажимает `Ctrl+Shift+Q`
2. Редактор перехватывает событие `keydown`
3. Находит плагин с `name === 'quote'`
4. Вызывает `execute()` – открывается модальное окно

---

## 🔄 Жизненный цикл плагина

### Инициализация

```typescript
ngOnInit(): void {
  // Редактор вызывает init() для каждого плагина
  this.plugins.forEach((plugin) => {
    if (plugin.init) {
      plugin.init();
    }
  });
}
```

**В QuotePlugin.init():**

- Проверяется, что код выполняется в браузере (SSR-safe)
- Загружается конфигурация из localStorage
- Настраиваются глобальные обработчики

### Выполнение (execute)

```typescript
execute(editorElement: HTMLElement, options?: Partial<QuoteModalOptions>): boolean {
  if (!this.isBrowser) return false;

  this.currentEditorElement = editorElement;

  // 1. Сохраняем позицию курсора
  this.saveSelection();

  // 2. Получаем выделенный текст (если есть)
  const selectedText = this.getSelectedText();

  // 3. Открываем модальное окно
  this.openQuoteModal({
    mode: 'insert',
    prefilledText: selectedText,
    savedSelection: this.savedRange || undefined,
    ...options,
  });

  return true;
}
```

### Уничтожение

```typescript
ngOnDestroy(): void {
  // Редактор вызывает destroy() для каждого плагина
  this.plugins.forEach((plugin) => {
    if (plugin.destroy) {
      plugin.destroy();
    }
  });
}
```

---

## 🎨 Подсветка активного состояния

Кнопка плагина подсвечивается, если курсор находится внутри `<blockquote>`:

```typescript
// QuotePlugin.isActive()
isActive(editorElement: HTMLElement): boolean {
  if (!this.isBrowser) return false;

  const selection = window.getSelection();
  if (!selection || selection.rangeCount === 0) return false;

  const node = selection.anchorNode;
  if (!node) return false;

  // Ищем родительский blockquote
  const blockquote = BlockquoteGenerator.findParentBlockquote(
    node.nodeType === Node.ELEMENT_NODE
      ? (node as HTMLElement)
      : node.parentElement!,
  );

  return blockquote !== null;
}
```

**В тулбаре:**

```typescript
isButtonActive(pluginName: string): boolean {
  return this.activePlugins.includes(pluginName);
}
```

```html
<button [class.active]="isButtonActive(plugin.name)" ...></button>
```

---

## 📦 Конфигурация плагина

**Файл:** `quote.config.ts`

```typescript
export const DEFAULT_QUOTE_CONFIG: QuotePluginConfig = {
  id: 'quote',
  name: 'quote',
  title: 'Цитата',
  description: 'Вставка стилизованных цитат',
  icon: '💬',
  hotkey: 'Ctrl+Shift+Q',
  // ... настройки UI, стилей, и т.д.
};
```

Эта конфигурация используется для:

- Отображения иконки в тулбаре
- Показа tooltip с горячей клавишей
- Инициализации дефолтных настроек модального окна

---

## 🚀 Запуск и тестирование

### 1. Запуск dev-сервера

```bash
npm start
# или
npm run start:ssl
```

### 2. Тестирование функциональности

**Через кнопку:**

1. Открыть редактор в браузере
2. Найти кнопку 💬 в тулбаре (в группе форматирования)
3. Кликнуть → Откроется модальное окно
4. Заполнить поля → Нажать "Вставить"
5. Цитата вставляется в редактор

**Через горячую клавишу:**

1. Поставить курсор в редактор (или выделить текст)
2. Нажать `Ctrl+Shift+Q` (или `Cmd+Shift+Q` на Mac)
3. Откроется модальное окно с предзаполненным текстом
4. Нажать "Вставить" → Цитата вставляется

**Подсветка активного состояния:**

1. Вставить цитату
2. Кликнуть внутри вставленной цитаты
3. Кнопка 💬 должна подсветиться (класс `.active`)

---

## 🔧 Изменённые файлы

### 1. `aurora-editor.component.ts`

**Изменения:**

- ❌ Удалён импорт `InsertQuotePlugin`
- ✅ Добавлен импорт `QuotePlugin`
- ✅ Изменено создание экземпляра: `new QuotePlugin()`
- ✅ Обновлён обработчик горячей клавиши `Ctrl+Shift+Q`

### 2. `aurora-toolbar.component.ts`

**Изменения:**

- ✅ Обновлён фильтр `formatPlugins`: `'insertQuote'` → `'quote'`

### 3. `quote.plugin.ts`

**Изменения:**

- ✅ Добавлено свойство `title: string` (для соответствия интерфейсу)
- ✅ Изменён тип возврата `execute()`: `void` → `boolean`

---

## ✅ Контрольный список интеграции

- [x] Импорт `QuotePlugin` в `aurora-editor.component.ts`
- [x] Создание экземпляра в массиве `plugins`
- [x] Вызов `init()` при инициализации редактора
- [x] Добавление плагина в категорию `formatPlugins` тулбара
- [x] Обработчик горячей клавиши `Ctrl+Shift+Q`
- [x] Отображение иконки 💬 в тулбаре
- [x] Tooltip с текстом "Цитата (Ctrl+Shift+Q)"
- [x] Подсветка активного состояния (`.active`)
- [x] Обработка клика по кнопке → `execute()`
- [x] Открытие модального окна при `execute()`
- [x] Вставка цитаты в редактор

---

## 🎯 Итог

Плагин **QuotePlugin** полностью интегрирован в Aurora Editor:

✅ **Кнопка в тулбаре** – Иконка 💬 в группе форматирования
✅ **Горячая клавиша** – `Ctrl+Shift+Q` открывает модальное окно
✅ **Активное состояние** – Подсветка кнопки при курсоре внутри цитаты
✅ **Модальное окно** – Полнофункциональный UI для создания цитат
✅ **Стили и теги** – 26 готовых стилей + система тегов
✅ **IndexedDB** – Оффлайн-хранилище стилей и избранных цитат

**Плагин готов к использованию!** 🚀
