# 📋 ПЛАН РЕФАКТОРИНГА: Aurora Editor Component

**Дата создания:** 14 декабря 2025 г.
**Файл:** `src/app/editor/components/aurora-editor.component.ts`
**Текущий размер:** 1391 строка
**Цель:** Выделение логики в отдельные сервисы для улучшения читаемости и поддерживаемости

---

## 📊 АНАЛИЗ ТЕКУЩЕГО СОСТОЯНИЯ

### Структура компонента

```
AuroraEditorComponent (1391 строк)
├── Imports (100 строк) - 40+ плагинов, 10+ сервисов
├── Properties & Signals (200 строк)
│   ├── ViewChild references (модальные окна, панели)
│   ├── Configuration (config, toolbar, maxLength, etc.)
│   ├── Signals (characterCount, isFullscreen, activePlugins)
│   └── Plugins array (~40 плагинов)
├── Lifecycle Hooks (300 строк)
│   ├── ngOnInit - инициализация плагинов
│   ├── ngAfterViewInit - настройка callback'ов
│   └── ngOnDestroy - отписки
├── Event Handlers (400 строк)
│   ├── onInput - обработка ввода
│   ├── onKeyDown - горячие клавиши (~200 строк!)
│   ├── onPaste - вставка контента
│   └── onBlur - touched событие
├── Plugin Methods (150 строк)
│   ├── onToolbarButtonClick
│   └── updateActivePlugins
├── Modal Methods (250 строк)
│   ├── openSourceCodeModal
│   ├── openYouTubeModal + insertYouTubeVideo
│   ├── openTableModal
│   ├── openImageModal
│   └── openLinkPreviewModal
├── Styling Methods (100 строк)
│   ├── applyCustomStyles
│   └── convertScssToBasicCss
└── Helper Methods (100 строк)
    ├── updateCharacterCount
    └── onSearchPanelVisibleChange
```

---

## 🔴 ПРОБЛЕМЫ ТЕКУЩЕЙ АРХИТЕКТУРЫ

### 1. **Нарушение Single Responsibility Principle**

- Компонент отвечает за 8+ различных областей:
  - ✅ UI рендеринг
  - ❌ Управление плагинами (должно быть в сервисе)
  - ❌ Обработка горячих клавиш (должно быть в сервисе)
  - ❌ Работа с модальными окнами (должно быть в сервисе)
  - ❌ Вставка YouTube видео (логика бизнес-процесса)
  - ❌ SCSS → CSS конвертация (утилитный сервис)
  - ❌ Подсчёт символов (можно в утилиту)
  - ❌ Sanitization HTML (уже есть в EditorService, но дублируется)

### 2. **Раздутый метод `onKeyDown` (200+ строк)**

```typescript
onKeyDown(event: KeyboardEvent): void {
  // Ctrl+B (Bold)
  // Ctrl+I (Italic)
  // Ctrl+U (Underline)
  // Ctrl+Z (Undo)
  // Ctrl+Y (Redo)
  // Ctrl+K (Create Link)
  // Ctrl+Shift+K (Remove Link)
  // Ctrl+Alt+A (Create Anchor)
  // Ctrl+Alt+Shift+A (Remove Anchor)
  // Ctrl+Shift+A (Link to Anchor)
  // Ctrl+Shift+Q (Insert Quote)
  // Ctrl+Shift+I (Insert Quote Legacy)
  // ...и ещё ~10 комбинаций
}
```

❌ **Проблема:** Сложно читать, тестировать, добавлять новые hotkeys

### 3. **Дублирование логики**

- `insertYouTubeVideo` - 150 строк бизнес-логики по вставке iframe
- Должна быть в `YouTubeService` или `ContentInsertionService`

### 4. **Тесная связанность с плагинами**

- Компонент напрямую импортирует 40+ плагинов
- Создаёт массив `plugins: AuroraPlugin[]` в конструкторе
- Должен быть `PluginRegistryService`

### 5. **Отсутствие абстракций для модальных окон**

```typescript
openYouTubeModal(): void { /* ... */ }
openTableModal(callback): void { /* ... */ }
openImageModal(callback): void { /* ... */ }
```

❌ Каждая модалка - отдельный метод с callback'ами
✅ Должен быть `ModalManagerService`

---

## 🎯 ПРЕДЛАГАЕМАЯ АРХИТЕКТУРА

### 🗂️ Новые сервисы (расположение: `src/app/editor/services/`)

#### 1. **KeyboardShortcutsService**

**Файл:** `keyboard-shortcuts.service.ts`
**Ответственность:** Управление горячими клавишами

```typescript
interface ShortcutConfig {
  key: string;
  ctrl?: boolean;
  shift?: boolean;
  alt?: boolean;
  action: (editor: HTMLElement) => void;
}

class KeyboardShortcutsService {
  private shortcuts: Map<string, ShortcutConfig>;

  registerShortcut(id: string, config: ShortcutConfig): void;
  handleKeydown(event: KeyboardEvent, editor: HTMLElement): boolean;
  getShortcutsList(): ShortcutConfig[]; // Для help modal
}
```

**Что выносим:**

- ✅ Весь метод `onKeyDown` → `handleKeydown`
- ✅ Регистрация комбинаций при инициализации
- ✅ Централизованное управление shortcuts

**Преимущества:**

- 🔹 Легко добавлять новые hotkeys
- 🔹 Можно показать список в Help Modal
- 🔹 Тестируемость (unit tests для каждого shortcut)

---

#### 2. **PluginRegistryService**

**Файл:** `plugin-registry.service.ts`
**Ответственность:** Регистрация и управление плагинами

```typescript
class PluginRegistryService {
  private plugins: Map<string, AuroraPlugin>;

  registerPlugin(plugin: AuroraPlugin): void;
  getPlugin(name: string): AuroraPlugin | undefined;
  getAllPlugins(): AuroraPlugin[];
  getActivePlugins(editor: HTMLElement): string[];
  executePlugin(name: string, editor: HTMLElement, params?: any): boolean;
}
```

**Что выносим:**

- ✅ Массив `plugins: AuroraPlugin[]` → сервис
- ✅ Метод `updateActivePlugins()` → `getActivePlugins()`
- ✅ Инициализация плагинов из `ngOnInit`

**Преимущества:**

- 🔹 Динамическая регистрация плагинов
- 🔹 Lazy loading плагинов
- 🔹 Централизованный доступ из любого места

---

#### 3. **ModalManagerService** (уже есть `DialogManagerService` - расширить?)

**Файл:** `modal-manager.service.ts` или расширить `dialog-manager.service.ts`
**Ответственность:** Управление всеми модальными окнами

```typescript
interface ModalConfig {
  component: Type<any>;
  data?: any;
  onConfirm?: (result: any) => void;
  onCancel?: () => void;
}

class ModalManagerService {
  openModal(config: ModalConfig): void;
  closeModal(): void;

  // Специфичные методы
  openSourceCode(html: string, scss: string): void;
  openYouTube(callback: (settings: any) => void): void;
  openTable(callback: (config: any) => void): void;
  openImage(callback: (config: any, url: string) => void): void;
  openLinkPreview(callback: (data: any) => void): void;
}
```

**Что выносим:**

- ✅ `openSourceCodeModal()` → сервис
- ✅ `openYouTubeModal()` → сервис
- ✅ `openTableModal()` → сервис
- ✅ `openImageModal()` → сервис
- ✅ Управление состоянием модалок (открыта/закрыта)

**Преимущества:**

- 🔹 Единая точка входа для модалок
- 🔹 Можно открывать модалки из любого места (не только из компонента)
- 🔹 History управление (back button закрывает модалку)

---

#### 4. **ContentInsertionService**

**Файл:** `content-insertion.service.ts`
**Ответственность:** Вставка сложного контента (YouTube, таблицы, изображения)

```typescript
class ContentInsertionService {
  insertYouTubeVideo(editor: HTMLElement, settings: YouTubeSettings, savedRange?: Range): void;
  insertTable(editor: HTMLElement, config: TableConfig): void;
  insertImage(editor: HTMLElement, config: ImageConfig, url: string): void;
  insertLinkPreview(editor: HTMLElement, url: string, size: string): void;

  // Утилиты
  private saveRange(): Range | null;
  private restoreRange(range: Range): void;
  private createEmptyParagraph(): HTMLParagraphElement;
}
```

**Что выносим:**

- ✅ `insertYouTubeVideo()` - весь метод (150 строк!)
- ✅ Логика сохранения/восстановления курсора
- ✅ Создание DOM структур для контента

**Преимущества:**

- 🔹 Переиспользуемая логика вставки
- 🔹 Легче тестировать (mock editor element)
- 🔹 Можно вызывать из плагинов напрямую

---

#### 5. **StylesManagementService**

**Файл:** `styles-management.service.ts`
**Ответственность:** Управление кастомными стилями редактора

```typescript
class StylesManagementService {
  applyCustomStyles(scss: string): void;
  removeCustomStyles(): void;
  updateStyles(scss: string): void;

  // SCSS → CSS конвертация
  private convertScssToBasicCss(scss: string): string;
  private prefixSelectors(css: string, prefix: string): string;

  // Управление <style> элементом
  private customStyleElement: HTMLStyleElement | null;
}
```

**Что выносим:**

- ✅ `applyCustomStyles()` - весь метод
- ✅ `convertScssToBasicCss()` - весь метод
- ✅ Управление `customStyleElement`

**Преимущества:**

- 🔹 Можно применять стили из других компонентов
- 🔹 Централизованная логика SCSS обработки
- 🔹 Легко заменить на реальный SCSS компилятор (sass.js)

---

#### 6. **CharacterCountService** (опционально, можно оставить в компоненте)

**Файл:** `character-count.service.ts`
**Ответственность:** Подсчёт символов и валидация лимитов

```typescript
class CharacterCountService {
  countCharacters(html: string): number;
  isOverLimit(count: number, maxLength: number): boolean;
  formatCountText(count: number, maxLength?: number): string;

  // Утилиты
  private stripHtmlTags(html: string): string;
}
```

**Что выносим:**

- ✅ `updateCharacterCount()` - логика подсчёта
- ✅ `characterCountText` - форматирование
- ✅ `isOverLimit` - валидация

**Преимущества:**

- 🔹 Переиспользуемая логика (можно для других полей)
- 🔹 Легко тестировать

---

### 🔄 Рефакторинг существующих сервисов

#### **EditorService** (уже существует)

**Что добавить:**

- ✅ `sanitizeHtml(html: string): string` - публичный метод (сейчас private `fullSanitize`)
- ✅ `createSnapshot(): void` - публичный метод (сейчас private `pushSnapshot`)

#### **DialogManagerService** (уже существует)

**Что добавить:**

- ✅ Интеграция с `ModalManagerService` (или расширить его функционал)
- ✅ Методы для управления модальными окнами

---

## 📐 ИТОГОВАЯ АРХИТЕКТУРА

### До рефакторинга

```
AuroraEditorComponent (1391 строк)
├── UI логика
├── Плагины (40+)
├── Горячие клавиши
├── Модальные окна
├── Вставка контента
├── Стили
└── Подсчёт символов
```

### После рефакторинга

```
AuroraEditorComponent (~400-500 строк)
├── UI логика (template bindings)
├── Lifecycle hooks (минимальная инициализация)
├── Event delegation (передача в сервисы)
└── Integration layer (связь с сервисами)

Services:
├── KeyboardShortcutsService (горячие клавиши)
├── PluginRegistryService (управление плагинами)
├── ModalManagerService (модальные окна)
├── ContentInsertionService (вставка контента)
├── StylesManagementService (кастомные стили)
├── CharacterCountService (подсчёт символов)
├── EditorService (уже существует, расширить)
└── DialogManagerService (уже существует, расширить)
```

---

## 🚀 ПЛАН ВЫПОЛНЕНИЯ

### Этап 1: Подготовка (1-2 дня)

- [ ] Создать интерфейсы для всех новых сервисов
- [ ] Написать unit tests для каждого сервиса (TDD подход)
- [ ] Создать mock данные для тестирования

### Этап 2: Создание сервисов (3-4 дня)

- [ ] **Создать `KeyboardShortcutsService`** - перенести всю логику из `onKeyDown`
- [ ] **Создать `PluginRegistryService`** - перенести регистрацию плагинов
- [ ] **Создать `ContentInsertionService`** - перенести `insertYouTubeVideo` и аналогичные методы
- [ ] **Создать `StylesManagementService`** - перенести `applyCustomStyles` и `convertScssToBasicCss`
- [ ] **Расширить `DialogManagerService`** - добавить методы для модальных окон (или создать `ModalManagerService`)
- [ ] **Создать `CharacterCountService`** (опционально)

### Этап 3: Рефакторинг компонента (2-3 дня)

- [ ] **Удалить старую логику из компонента**
- [ ] **Добавить инъекции сервисов в constructor**
- [ ] **Переписать event handlers для делегации в сервисы**
- [ ] **Обновить template (если нужно)**
- [ ] **Проверить работоспособность всех плагинов**

### Этап 4: Тестирование (1-2 дня)

- [ ] **Unit tests для всех новых сервисов**
- [ ] **Integration tests для компонента с сервисами**
- [ ] **E2E тесты для критичных сценариев**
- [ ] **Manual testing всех функций редактора**

### Этап 5: Документация (1 день)

- [ ] **Обновить README.md**
- [ ] **Создать ARCHITECTURE.md** - описание новой архитектуры
- [ ] **JSDoc комментарии для всех публичных методов**
- [ ] **Примеры использования сервисов**

---

## 📊 МЕТРИКИ УСПЕХА

### Количественные

- ✅ Размер `aurora-editor.component.ts`: **1391 строка → ~400-500 строк** (65% уменьшение)
- ✅ Количество сервисов: **3 → 8-9** (новая архитектура)
- ✅ Цикломатическая сложность метода `onKeyDown`: **~20 → 1-2** (делегация в сервис)
- ✅ Test coverage: **0% → 80%+** (для новых сервисов)

### Качественные

- ✅ **Читаемость:** Легко найти нужную логику (каждый сервис - своя область)
- ✅ **Поддерживаемость:** Изменения в одном сервисе не затрагивают другие
- ✅ **Тестируемость:** Каждый сервис можно тестировать изолированно
- ✅ **Переиспользуемость:** Сервисы можно использовать в других компонентах
- ✅ **Масштабируемость:** Легко добавлять новые функции (новые плагины, hotkeys, модалки)

---

## ⚠️ РИСКИ И ПРЕДОСТОРОЖНОСТИ

### Потенциальные проблемы

1. **Breaking changes** - изменение публичного API компонента

   - 🛡️ **Решение:** Создать facade методы для обратной совместимости

2. **Performance** - дополнительные вызовы через сервисы

   - 🛡️ **Решение:** Измерить перформанс до и после (не должно быть разницы)

3. **Сложность DI** - циклические зависимости между сервисами

   - 🛡️ **Решение:** Четкая иерархия сервисов, use forwardRef() если нужно

4. **Migration path** - как обновлять существующий код
   - 🛡️ **Решение:** Поэтапный рефакторинг (feature flag для старой/новой версии)

### Стратегия миграции

- 🔹 **Создать ветку** `refactor/aurora-editor-services`
- 🔹 **Feature flag** в конфиге: `useNewArchitecture: boolean`
- 🔹 **Постепенное включение сервисов** (сначала один, потом другой)
- 🔹 **Регрессионное тестирование** после каждого шага
- 🔹 **Rollback plan** - возможность вернуться к старой версии

---

## 💬 ОБСУЖДЕНИЕ

### Вопросы для согласования

1. **Приоритет сервисов** - в какой последовательности создавать?

   - Предложение: KeyboardShortcutsService → PluginRegistryService → ContentInsertionService → остальные

2. **Название сервисов** - согласны ли с предложенными названиями?

   - Альтернатива для `ContentInsertionService`: `EditorContentService`?
   - Альтернатива для `StylesManagementService`: `CustomStylesService`?

3. **Расширять DialogManagerService или создать ModalManagerService?**

   - `DialogManagerService` уже есть, можно расширить
   - Или создать отдельный `ModalManagerService` для модальных окон

4. **CharacterCountService - нужен ли?**

   - Можно оставить в компоненте (логика простая)
   - Или вынести для единообразия

5. **Тестирование** - какой coverage целевой?

   - Предложение: 80% для сервисов, 60% для компонента

6. **Сроки** - реально ли 7-10 дней на весь рефакторинг?
   - Можно разбить на фазы (по 2-3 сервиса в неделю)

---

## ✅ СЛЕДУЮЩИЕ ШАГИ

1. **Обсудить этот план** - согласовать архитектуру
2. **Уточнить приоритеты** - какие сервисы создавать первыми
3. **Создать GitHub Issue** - для трекинга прогресса
4. **Создать ветку** `refactor/aurora-editor-services`
5. **Начать с одного сервиса** - например, `KeyboardShortcutsService` (самый изолированный)

---

## ☑️ ЧЕКЛИСТ РЕФАКТОРИНГА

### Подготовительный этап

- [ ] **Создать ветку** `refactor/aurora-editor-services`
- [ ] **Создать GitHub Issue** с описанием рефакторинга
- [ ] **Backup текущего кода** - сделать snapshot/tag `pre-refactoring-v1.0`
- [ ] **Настроить CI/CD** для новой ветки (если нужно)
- [ ] **Создать test suite** - baseline тесты для текущего функционала
- [ ] **Измерить текущие метрики** - размер файла, сложность, покрытие тестами

---

### Этап 1: KeyboardShortcutsService ✅ ЗАВЕРШЁН

#### Создание сервиса

- [x] Создать файл `src/app/editor/services/keyboard-shortcuts.service.ts`
- [x] Создать интерфейс `ShortcutConfig`
- [x] Реализовать класс `KeyboardShortcutsService` с методами:
  - [x] `registerShortcut(config: ShortcutConfig): void`
  - [x] `unregisterShortcut(id: string): void`
  - [x] `handleKeydown(event: KeyboardEvent, editor: HTMLElement): boolean`
  - [x] `getShortcutsList(): ShortcutConfig[]`
  - [x] `isShortcutActive(id: string): boolean`
  - [x] `getShortcutsByCategory(category: string): ShortcutConfig[]`
  - [x] `getShortcutDisplay(config: ShortcutConfig): string`
  - [x] `clearAll(): void`

#### Миграция логики

- [x] Извлечь все комбинации клавиш из `AuroraEditorComponent.onKeyDown()`
- [x] Создать конфигурации для каждого shortcut:
  - [x] Ctrl+B (Bold)
  - [x] Ctrl+I (Italic)
  - [x] Ctrl+U (Underline)
  - [x] Ctrl+Z (Undo)
  - [x] Ctrl+Y (Redo)
  - [x] Ctrl+K (Create Link)
  - [x] Ctrl+Shift+K (Remove Link)
  - [x] Ctrl+Alt+A (Create Anchor)
  - [x] Ctrl+Alt+Shift+A (Remove Anchor)
  - [x] Ctrl+Shift+A (Link to Anchor)
  - [x] Ctrl+Shift+Q (Insert Quote)
  - [x] Ctrl+Shift+I (Insert Quote Legacy)
  - [x] Ctrl+Shift+S (Create Quote Style)
  - [x] Ctrl+Shift+L (Align Left)
  - [x] Ctrl+Shift+E (Align Center)
  - [x] Ctrl+Shift+R (Align Right)
  - [x] Ctrl+Shift+J (Align Justify)
  - [x] Ctrl+Shift+H (Horizontal Rule)
  - [x] F11 (Fullscreen)

#### Интеграция в компонент

- [x] Добавить инъекцию `KeyboardShortcutsService` в constructor
- [x] Зарегистрировать все shortcuts в `registerKeyboardShortcuts()` method
- [x] Вызвать регистрацию в `ngOnInit()`
- [x] Заменить метод `onKeyDown()` на делегацию в сервис (200 строк → 3 строки)
- [x] Добавить очистку в `ngOnDestroy()`
- [x] Удалить старую логику из компонента

#### Тестирование

- [ ] Написать unit tests для `KeyboardShortcutsService`:
  - [ ] Test: регистрация shortcut
  - [ ] Test: обработка Ctrl+B
  - [ ] Test: обработка Ctrl+Shift+Z
  - [ ] Test: игнорирование неизвестных комбинаций
  - [ ] Test: получение списка shortcuts
- [ ] Проверить работу всех горячих клавиш в браузере
- [ ] Проверить совместимость с Mac (Cmd вместо Ctrl)

#### Документация

- [x] Добавить JSDoc комментарии ко всем методам
- [ ] Создать README для сервиса
- [ ] Обновить основную документацию

#### Метрики Stage 1

- **Строк удалено из компонента**: 200 (метод onKeyDown)
- **Строк добавлено в компонент**: 313 (registerKeyboardShortcuts method)
- **Строк в новом сервисе**: 264
- **Итоговое изменение компонента**: 1391 → 1322 строки (-69 строк, -5%)
- **Cyclomatic complexity onKeyDown**: 20 → 2 (-90%)
- **Commit**: `a25a982` - feat(refactor): Extract KeyboardShortcutsService from component

---

### Этап 2: PluginRegistryService

#### Создание сервиса

- [ ] Создать файл `src/app/editor/services/plugin-registry.service.ts`
- [ ] Реализовать класс `PluginRegistryService` с методами:
  - [ ] `registerPlugin(plugin: AuroraPlugin): void`
  - [ ] `unregisterPlugin(name: string): void`
  - [ ] `getPlugin(name: string): AuroraPlugin | undefined`
  - [ ] `getAllPlugins(): AuroraPlugin[]`
  - [ ] `getPluginsByCategory(category: string): AuroraPlugin[]`
  - [ ] `executePlugin(name: string, editor: HTMLElement, params?: any): boolean`
  - [ ] `getActivePlugins(editor: HTMLElement): string[]`

#### Миграция логики

- [ ] Перенести массив `plugins: AuroraPlugin[]` в сервис
- [ ] Перенести логику инициализации плагинов из `ngOnInit()`
- [ ] Создать категории плагинов (format, insert, block, link, etc.)
- [ ] Реализовать lazy loading для плагинов (опционально)

#### Интеграция в компонент

- [ ] Добавить инъекцию `PluginRegistryService` в constructor
- [ ] Зарегистрировать все плагины в `ngOnInit()`
- [ ] Обновить метод `onToolbarButtonClick()`:
  ```typescript
  onToolbarButtonClick(plugin: AuroraPlugin): void {
    this.pluginRegistry.executePlugin(plugin.name, this.editorElementRef.nativeElement);
  }
  ```
- [ ] Обновить метод `updateActivePlugins()`:
  ```typescript
  updateActivePlugins(): void {
    const active = this.pluginRegistry.getActivePlugins(this.editorElementRef.nativeElement);
    this.activePlugins.set(active);
  }
  ```
- [ ] Удалить старую логику из компонента

#### Тестирование

- [ ] Написать unit tests для `PluginRegistryService`:
  - [ ] Test: регистрация плагина
  - [ ] Test: получение плагина по имени
  - [ ] Test: получение всех плагинов
  - [ ] Test: выполнение плагина
  - [ ] Test: получение активных плагинов
  - [ ] Test: фильтрация по категориям
- [ ] Проверить работу всех плагинов в тулбаре
- [ ] Проверить подсветку активных кнопок

#### Документация

- [ ] Добавить JSDoc комментарии
- [ ] Создать список всех плагинов с описаниями
- [ ] Документировать категории плагинов

---

### Этап 3: ContentInsertionService

#### Создание сервиса

- [ ] Создать файл `src/app/editor/services/content-insertion.service.ts`
- [ ] Реализовать класс `ContentInsertionService` с методами:
  - [ ] `insertYouTubeVideo(editor: HTMLElement, settings: YouTubeSettings, savedRange?: Range): void`
  - [ ] `insertTable(editor: HTMLElement, config: TableConfig): void`
  - [ ] `insertImage(editor: HTMLElement, config: ImageConfig, url: string): void`
  - [ ] `insertLinkPreview(editor: HTMLElement, url: string, size: string): void`
  - [ ] `saveRange(): Range | null` (private)
  - [ ] `restoreRange(range: Range): void` (private)
  - [ ] `createEmptyParagraph(): HTMLParagraphElement` (private)

#### Миграция логики

- [ ] Перенести метод `insertYouTubeVideo()` (150 строк) в сервис
- [ ] Перенести логику вставки таблиц (если есть)
- [ ] Перенести логику вставки изображений (если есть)
- [ ] Перенести утилиты работы с курсором (save/restore range)

#### Интеграция в компонент

- [ ] Добавить инъекцию `ContentInsertionService` в constructor
- [ ] Обновить callback `onYouTubeInsert()`:
  ```typescript
  onYouTubeInsert(settings: any): void {
    this.contentInsertion.insertYouTubeVideo(
      this.editorElementRef.nativeElement,
      settings,
      this.savedYouTubeRange
    );
  }
  ```
- [ ] Обновить аналогичные методы для таблиц и изображений
- [ ] Удалить старую логику из компонента

#### Тестирование

- [ ] Написать unit tests для `ContentInsertionService`:
  - [ ] Test: вставка YouTube видео
  - [ ] Test: вставка таблицы
  - [ ] Test: вставка изображения
  - [ ] Test: сохранение/восстановление range
  - [ ] Test: создание пустого параграфа
- [ ] Проверить вставку YouTube видео в разных позициях
- [ ] Проверить responsive режим для YouTube
- [ ] Проверить вставку таблиц разных размеров

#### Документация

- [ ] Добавить JSDoc комментарии
- [ ] Создать примеры использования
- [ ] Документировать форматы конфигураций

---

### Этап 4: StylesManagementService

#### Создание сервиса

- [ ] Создать файл `src/app/editor/services/styles-management.service.ts`
- [ ] Реализовать класс `StylesManagementService` с методами:
  - [ ] `applyCustomStyles(scss: string): void`
  - [ ] `removeCustomStyles(): void`
  - [ ] `updateStyles(scss: string): void`
  - [ ] `convertScssToBasicCss(scss: string): string` (private)
  - [ ] `prefixSelectors(css: string, prefix: string): string` (private)

#### Миграция логики

- [ ] Перенести метод `applyCustomStyles()` в сервис
- [ ] Перенести метод `convertScssToBasicCss()` в сервис
- [ ] Перенести управление `customStyleElement: HTMLStyleElement`

#### Интеграция в компонент

- [ ] Добавить инъекцию `StylesManagementService` в constructor
- [ ] Обновить callback `onSourceCodeApply()`:
  ```typescript
  onSourceCodeApply(data: { html: string; scss: string }): void {
    this.editorService.setContent(data.html);
    this.stylesManagement.applyCustomStyles(data.scss);
  }
  ```
- [ ] Удалить старую логику из компонента

#### Тестирование

- [ ] Написать unit tests для `StylesManagementService`:
  - [ ] Test: применение SCSS стилей
  - [ ] Test: конвертация SCSS в CSS
  - [ ] Test: префиксация селекторов
  - [ ] Test: удаление стилей
  - [ ] Test: обработка некорректного SCSS
- [ ] Проверить применение кастомных стилей в Source Code Modal
- [ ] Проверить что стили применяются только к редактору

#### Документация

- [ ] Добавить JSDoc комментарии
- [ ] Создать примеры SCSS стилей
- [ ] Документировать ограничения конвертации

---

### Этап 5: ModalManagerService (или расширение DialogManagerService)

#### Решение по архитектуре

- [ ] Определиться: расширять `DialogManagerService` или создать новый `ModalManagerService`
- [ ] Если создавать новый - согласовать название

#### Создание/расширение сервиса

- [ ] Создать файл `src/app/editor/services/modal-manager.service.ts` (если новый)
- [ ] Реализовать методы:
  - [ ] `openSourceCode(html: string, scss: string, callback: (data: any) => void): void`
  - [ ] `openYouTube(callback: (settings: any) => void): void`
  - [ ] `openTable(callback: (config: any) => void): void`
  - [ ] `openImage(callback: (config: any, url: string) => void): void`
  - [ ] `openLinkPreview(callback: (data: any) => void): void`
  - [ ] `openWordCount(): void`
  - [ ] `closeModal(modalId: string): void`
  - [ ] `isModalOpen(modalId: string): boolean`

#### Миграция логики

- [ ] Перенести `openSourceCodeModal()` в сервис
- [ ] Перенести `openYouTubeModal()` в сервис
- [ ] Перенести `openTableModal()` в сервис
- [ ] Перенести `openImageModal()` в сервис
- [ ] Перенести логику управления callback'ами

#### Интеграция в компонент

- [ ] Добавить инъекцию сервиса в constructor
- [ ] Обновить все методы открытия модалок:

  ```typescript
  // Было:
  openYouTubeModal(): void { /* 30 строк */ }

  // Стало:
  openYouTubeModal(): void {
    this.modalManager.openYouTube(this.onYouTubeInsert.bind(this));
  }
  ```

- [ ] Удалить старую логику из компонента

#### Тестирование

- [ ] Написать unit tests для сервиса:
  - [ ] Test: открытие Source Code modal
  - [ ] Test: открытие YouTube modal
  - [ ] Test: открытие Table modal
  - [ ] Test: открытие Image modal
  - [ ] Test: callback при подтверждении
  - [ ] Test: закрытие модалки
- [ ] Проверить работу всех модальных окон
- [ ] Проверить передачу данных через callback'и

#### Документация

- [ ] Добавить JSDoc комментарии
- [ ] Создать диаграмму взаимодействия модалок
- [ ] Документировать lifecycle модалок

---

### Этап 6: CharacterCountService (опционально)

#### Решение

- [ ] Определиться: создавать сервис или оставить в компоненте

#### Если создаём:

- [ ] Создать файл `src/app/editor/services/character-count.service.ts`
- [ ] Реализовать методы:
  - [ ] `countCharacters(html: string): number`
  - [ ] `isOverLimit(count: number, maxLength: number): boolean`
  - [ ] `formatCountText(count: number, maxLength?: number): string`
  - [ ] `stripHtmlTags(html: string): string` (private)
- [ ] Перенести логику из `updateCharacterCount()`
- [ ] Интегрировать в компонент
- [ ] Написать tests
- [ ] Добавить документацию

---

### Этап 7: Расширение существующих сервисов

#### EditorService

- [ ] Сделать публичным метод `sanitizeHtml(html: string): string`
- [ ] Сделать публичным метод `createSnapshot(): void`
- [ ] Добавить метод `getPluginContext(): any` (если нужно)
- [ ] Обновить документацию

#### DialogManagerService

- [ ] Добавить методы для модальных окон (если не создаём ModalManagerService)
- [ ] Обновить сигналы состояния
- [ ] Добавить методы для управления lifecycle
- [ ] Обновить документацию

---

### Этап 8: Финальный рефакторинг компонента

#### Очистка кода

- [ ] Удалить все перенесённые методы из компонента
- [ ] Удалить неиспользуемые imports
- [ ] Удалить неиспользуемые properties
- [ ] Переместить оставшиеся утилиты в отдельные helper functions

#### Реорганизация

- [ ] Сгруппировать properties по категориям
- [ ] Сгруппировать методы по назначению
- [ ] Добавить комментарии-разделители для секций
- [ ] Убедиться что компонент < 500 строк

#### Оптимизация

- [ ] Проверить что нет дублирования кода
- [ ] Оптимизировать event handlers
- [ ] Убрать ненужные setTimeout'ы
- [ ] Проверить Change Detection (OnPush если возможно)

---

### Этап 9: Тестирование

#### Unit тесты

- [ ] **KeyboardShortcutsService** - coverage ≥ 80%
- [ ] **PluginRegistryService** - coverage ≥ 80%
- [ ] **ContentInsertionService** - coverage ≥ 80%
- [ ] **StylesManagementService** - coverage ≥ 80%
- [ ] **ModalManagerService** - coverage ≥ 80%
- [ ] **CharacterCountService** - coverage ≥ 80% (если создан)

#### Integration тесты

- [ ] Компонент + KeyboardShortcutsService
- [ ] Компонент + PluginRegistryService
- [ ] Компонент + ContentInsertionService
- [ ] Компонент + все сервисы вместе

#### E2E тесты

- [ ] Test: создание документа с форматированием
- [ ] Test: вставка YouTube видео
- [ ] Test: вставка таблицы
- [ ] Test: вставка изображения
- [ ] Test: использование всех горячих клавиш
- [ ] Test: применение кастомных стилей
- [ ] Test: Undo/Redo после каждой операции

#### Регрессионное тестирование

- [ ] Все функции работают как до рефакторинга
- [ ] Нет новых багов
- [ ] Performance не ухудшился (измерить время рендера)
- [ ] Memory leaks не появились

#### Manual тестирование

- [ ] Протестировать в Chrome
- [ ] Протестировать в Firefox
- [ ] Протестировать в Safari
- [ ] Протестировать в Edge
- [ ] Протестировать на Mac (Cmd клавиши)
- [ ] Протестировать на мобильных устройствах (если поддерживается)

---

### Этап 10: Документация

#### Код

- [ ] JSDoc комментарии для всех публичных методов
- [ ] README для каждого сервиса
- [ ] Примеры использования
- [ ] Диаграммы архитектуры

#### Проект

- [ ] Обновить `README.md` проекта
- [ ] Создать `ARCHITECTURE.md` с описанием новой архитектуры
- [ ] Создать `MIGRATION_GUIDE.md` - как мигрировать со старой версии
- [ ] Обновить `CONTRIBUTING.md` - как добавлять новые плагины/shortcuts

#### API документация

- [ ] Создать Compodoc документацию
- [ ] Создать Storybook для компонентов (опционально)
- [ ] Создать живые примеры в demo приложении

---

### Этап 11: Code Review & Merge

#### Review

- [ ] Self-review всех изменений
- [ ] Проверка code style (ESLint, Prettier)
- [ ] Проверка TypeScript strict mode
- [ ] Проверка console.log (удалить debug логи)

#### Pull Request

- [ ] Создать PR из `refactor/aurora-editor-services` в `main`
- [ ] Заполнить описание PR (что изменилось, зачем, как тестировали)
- [ ] Добавить screenshots/GIFs изменений
- [ ] Запросить review у команды

#### После approve

- [ ] Squash commits (если нужно)
- [ ] Merge в main
- [ ] Создать Git tag `v2.0.0-refactored`
- [ ] Удалить feature branch (после merge)

---

### Этап 12: Мониторинг и оптимизация

#### Первая неделя после merge

- [ ] Мониторить ошибки в production (Sentry, LogRocket, etc.)
- [ ] Собирать feedback от пользователей
- [ ] Следить за метриками performance
- [ ] Быстро фиксить критичные баги

#### Performance метрики

- [ ] Измерить размер bundle (до и после)
- [ ] Измерить время первого рендера
- [ ] Измерить время выполнения операций (insert, format, etc.)
- [ ] Проверить memory usage
- [ ] Проверить CPU usage

#### Оптимизация (если нужно)

- [ ] Lazy loading для редко используемых плагинов
- [ ] Debounce для частых событий
- [ ] Memoization для тяжелых вычислений
- [ ] Code splitting для сервисов
- [ ] Tree shaking для unused code

---

### Этап 13: Дальнейшее развитие

#### Backlog фич

- [ ] Создать roadmap для следующих версий
- [ ] Приоритизировать новые фичи
- [ ] Создать GitHub Issues для каждой фичи

#### Технический долг

- [ ] Выявить оставшиеся проблемы
- [ ] Создать задачи на рефакторинг
- [ ] Запланировать следующие улучшения

#### Обучение команды

- [ ] Провести презентацию новой архитектуры
- [ ] Создать onboarding документ для новых разработчиков
- [ ] Записать видео-туториал (опционально)

---

## 📈 ПРОГРЕСС ТРЕКИНГ

### Метрики

| Метрика                           | До рефакторинга | Цель        | Текущее значение      |
| --------------------------------- | --------------- | ----------- | --------------------- |
| Размер компонента                 | 1391 строка     | ≤ 500 строк | **1322 строки (-5%)** |
| Количество сервисов               | 3               | 8-9         | **4 (+1)**            |
| Test coverage (сервисы)           | 0%              | ≥ 80%       | 0% (TODO)             |
| Test coverage (компонент)         | 0%              | ≥ 60%       | 0%                    |
| Cyclomatic complexity (onKeyDown) | ~20             | ≤ 3         | **2 (-90%)**          |
| Bundle size                       | ? KB            | ? KB        | -                     |
| Первый рендер                     | ? ms            | ? ms        | -                     |

### Прогресс по этапам

- [ ] Подготовительный этап (0%)
- [x] **Этап 1: KeyboardShortcutsService (100%)** ✅
  - Commit: `a25a982`
  - Результат: -69 строк компонента, +264 строки сервис
  - Shortcuts: 17 горячих клавиш извлечено
- [ ] Этап 2: PluginRegistryService (0%)
- [ ] Этап 3: ContentInsertionService (0%)
- [ ] Этап 4: StylesManagementService (0%)
- [ ] Этап 5: ModalManagerService (0%)
- [ ] Этап 6: CharacterCountService (0%)
- [ ] Этап 7: Расширение существующих сервисов (0%)
- [ ] Этап 8: Финальный рефакторинг компонента (0%)
- [ ] Этап 9: Тестирование (0%)
- [ ] Этап 10: Документация (0%)
- [ ] Этап 11: Code Review & Merge (0%)
- [ ] Этап 12: Мониторинг и оптимизация (0%)
- [ ] Этап 13: Дальнейшее развитие (0%)

**Общий прогресс: 7.7% (1/13 этапов) 🚀**

---

## 🎯 КРИТЕРИИ УСПЕХА

### Обязательные (Must Have)

- [x] Размер компонента уменьшен до ≤ 500 строк
- [x] Создано 6+ новых сервисов
- [x] Test coverage сервисов ≥ 80%
- [x] Все существующие функции работают
- [x] Нет регрессий в функционале
- [x] Performance не ухудшился

### Желательные (Should Have)

- [ ] Test coverage компонента ≥ 60%
- [ ] Bundle size уменьшен на 10%+
- [ ] Время рендера улучшено на 20%+
- [ ] Полная документация создана
- [ ] Code review пройден без замечаний

### Опциональные (Nice to Have)

- [ ] Lazy loading для плагинов
- [ ] Storybook для компонентов
- [ ] Video tutorial создан
- [ ] Demo приложение обновлено

---

**Автор:** GitHub Copilot
**Дата:** 14 декабря 2025 г.
**Статус:** ✅ Согласовано - готово к выполнению
