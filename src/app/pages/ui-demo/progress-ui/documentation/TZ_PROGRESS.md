# 📊 Progress Bar Component (v2.0)

Техническое задание на разработку высокофункционального компонента индикации прогресса.

## 🎯 Цели и задачи

Создать гибкий, типизированный и доступный (a11y) компонент для отображения прогресса выполнения задач в различных форматах (линия, круг, дашборд).

## ⚙️ Техническая спецификация

### 1. Основные типы (Interfaces & Types)

```typescript
/** Типы отображения */
export type ProgressType = 'line' | 'circle' | 'dashboard';

/** Статусы выполнения */
export type ProgressStatus = 'normal' | 'active' | 'success' | 'error' | 'warning';

/** Предустановленные размеры */
export type ProgressSize = 'small' | 'default' | 'large';

/** Линейные и радиальные градиенты */
export interface ProgressGradient {
  from: string;
  to: string;
  direction?: string | number;
  stops?: Array<{ color: string; position: number }>;
}

/** Сегментированный прогресс */
export interface ProgressSteps {
  total: number;
  current: number;
  gap?: number;
}
```

### 2. Входные параметры (Inputs)

| Параметр        | Тип                                      | По умолчанию   | Описание                            |
| :-------------- | :--------------------------------------- | :------------- | :---------------------------------- |
| `percent`       | `number`                                 | `0`            | Значение 0-100.                     |
| `type`          | `ProgressType`                           | `'line'`       | Тип визуализации.                   |
| `status`        | `ProgressStatus`                         | `'normal'`     | Текущий статус.                     |
| `size`          | `ProgressSize \| number`                 | `'default'`    | Размер или кастомные габариты.      |
| `strokeWidth`   | `number`                                 | `8`            | Толщина основной линии.             |
| `strokeColor`   | `string \| string[] \| ProgressGradient` | `-`            | Цвет или градиент.                  |
| `trailColor`    | `string`                                 | `'#f5f5f5'`    | Цвет подложки.                      |
| `showInfo`      | `boolean`                                | `true`         | Показывать ли текст/иконку.         |
| `format`        | `(p: number) => string`                  | `p => p + '%'` | Форматирование вывода.              |
| `indeterminate` | `boolean`                                | `false`        | Режим бесконечной загрузки.         |
| `gapDegree`     | `number`                                 | `75`           | Угол разрыва для dashboard (0-295). |

### 3. Доступность (A11y)

Компонент должен соответствовать стандартам W3C WAI-ARIA:

- `role="progressbar"`
- `aria-valuenow`, `aria-valuemin`, `aria-valuemax`
- Поддержка высокого контраста.
- Уведомление скринридеров об изменениях (`aria-live`).

### 4. Анимация и интерактивность

- Плавные переходы при изменении `percent`.
- Анимация "активного" состояния (пульсирующая полоса или полоски).
- Поддержка событий `onPercentChange`, `onComplete`.

## 🛠️ Валидация данных

1. `percent` должен принудительно ограничиваться диапазоном `[0, 100]`.
2. При `type="dashboard"`, `gapDegree` ограничен `0-295`.
3. Кастомные числовые размеры (`size`) должны быть положительными.

## 🎨 Темы

Поддержка как минимум двух встроенных тем: `light` (стандартная) и `dark`.
Цвета статусов определяются CSS-переменными для легкой темизации.
