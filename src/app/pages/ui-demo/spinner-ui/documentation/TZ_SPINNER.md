# 🧶 Spinner (Loader) Component

Компонент для индикации процессов загрузки. Поддерживает различные анимации, размеры и режимы перекрытия (overlay).

## 🚀 Как использовать

### 1. Импорт

```typescript
import { AvSpinnerComponent } from '@shared/components/ui/spinner';

@Component({
  imports: [AvSpinnerComponent]
})
```

### 2. Базовый пример

```html
<av-spinner></av-spinner>
```

### 3. С подписью и описанием

```html
<av-spinner
  label="Загрузка данных"
  tip="Это может занять несколько секунд"
  color="primary"
></av-spinner>
```

### 4. Режимы Overlay

```html
<!-- Внутри контейнера (соседние элементы будут перекрыты) -->
<div style="position: relative; height: 100px;">
  <av-spinner [overlay]="true" label="Обновление..."></av-spinner>
</div>

<!-- На весь экран -->
<av-spinner [fullScreen]="true" label="Инициализация приложения"></av-spinner>
```

### 5. Управление видимостью

Параметр `visible` позволяет скрывать спиннер плавной анимацией (fade-out) без его удаления из DOM. Это полезно для предотвращения скачков интерфейса:

```html
<av-spinner [visible]="!dataLoaded()" [delay]="500"></av-spinner>
```

## ⚙️ Настройки (Inputs)

| Параметр          | Тип                   | По умолчанию | Описание                                                                |
| ----------------- | --------------------- | ------------ | ----------------------------------------------------------------------- |
| `visible`         | `boolean`             | `true`       | Плавное отображение/скрытие компонента                                  |
| `size`            | `preset \| number`    | `'default'`  | Размеры: tiny, small, default, large, huge или px                       |
| `color`           | `preset \| string`    | `'primary'`  | Цвет: primary, success, warning, error, neutral, white, inherit или HEX |
| `animation`       | `'spin' \| 'pulse'`   | `'spin'`     | Тип анимации                                                            |
| `label`           | `string`              | `''`         | Основной текст подписи                                                  |
| `tip`             | `string`              | `''`         | Вспомогательный текст (серый)                                           |
| `labelPosition`   | `'bottom' \| 'right'` | `'bottom'`   | Расположение текста относительно иконки                                 |
| `overlay`         | `boolean`             | `false`      | Режим перекрытия в родительском блоке                                   |
| `backdropOpacity` | `number`              | `0.6`        | Прозрачность фона оверлея                                               |
| `backdropBlur`    | `number`              | `2`          | Размытие фона оверлея (px)                                              |
| `delay`           | `number`              | `0`          | Задержка появления (мс)                                                 |
