/**
 * Режим уведомлений о входе в систему
 */
export enum LoginNotificationMode {
  /** Никогда не уведомлять */
  Never = 0,
  /** Уведомлять только при входе с нового устройства */
  NewDeviceOnly = 1,
  /** Всегда уведомлять о входе */
  Always = 2,
}
