- Создние нового компонента для UI Demo ПУТЕМ РЕФАКТОРИНГА
  Для этого выполни следующее
  Прочитай план рефакторинга ui-component-refactor.md
  прочитай пример
  src\app\pages\ui-demo\button-control-aurora
  прочитай
  src\app\pages\ui-demo\pagination-ui
  НА ОСНОВАНИИ ЭТОГО НАДО СДЕЛАТЬ РЕФАКТОРИНГ
  КОМПОНЕНТА
  src\app\pages\ui-demo\pagination-ui
  в КОМПОНЕНТ
  src\app\pages\ui-demo\pagination-control-aurora
  ПРИ РЕФАКТОРИНГЕ
  не пропускай создание блоков 🎮 Playground 💻 Код 📚 Примеры 📖 API
  не пропускай создание документации Блок 📚 "Примеры использования" Блок 📖 Документация
  ЗАДАЧА 1
  Преобразовать компонент в унифицированный формат Aurora Control с использованием:

✅ AvShowcaseComponent (единая структура)
✅ ControlDocumentationComponent (централизованная документация)
✅ Angular Signals (реактивное состояние)
✅ Интерактивный Playground с генерацией кода

СОСТАВЬ план рефакторинга - РЕФАКТОРИНГ НЕ ДЕЛАЙ Я ПРОВЕРЮ ПЛАН

УЧТИ ЧТО файлы и роуты УЖЕ СОЗДАНЫ
ДЛЯ НОВОГО КОМПОНЕНТА
src\app\pages\ui-demo\pagination-control-aurora

=============================================================
📋 План рефакторинга: Pagination Control Aurora

1. Аналитика и Сбор Данных
   Целевой компонент:
   PaginationComponent
   (av-pagination).
   Inputs: total, variant, size, showSizeChanger, pageSizeOptions, showQuickJumper, showTotal, maxPages, disabled, hideOnSinglePage, locale, shape, color, showFirstLast.
   Models: currentPage, pageSize.
   Outputs: paginationChange.
2. Создание конфигурации (
   .config.ts
   )
   Создать файл pagination-control-aurora.config.ts, в котором описать:

Секция API: Полный перечень всех 14 входящих параметров и 3 событий.
Примеры использования:
Базовая пагинация.
Варианты (Simple, Compact, Minimal).
Размеры (Small, Medium, Large).
Функции (Выбор размера страницы, Быстрый переход).
Архитектурные заметки: Описание использования Angular 19 model(), реактивности на сигналах и соответствия стандартам доступности (WCAG). 3. Логика компонента (
.component.ts
)
State Management: Замена разрозненных сигналов (playgroundPage, playgroundSize и т.д.) на единый объект состояния config = signal<PaginationConfig>.
Генерация кода: Реализация computed(() => generatedCode), который будет возвращать два блока:
html
: Чистый декларативный шаблон.
typescript: Пример компонента с обработчиком
PaginationChangeEvent
.
Showcase Config: Настройка showcaseConfig с разделением колонок [14, 10] и описанием блоков результата. 4. Шаблон компонента (
.component.html
)
Использование структуры av-showcase с 4-мя вкладками:

🎮 Playground:
Блок "Основные настройки" (Total, CurrentPage, PageSize).
Блок "Внешний вид" (Размер, Вариант, Форма, Цвет) — с использованием радиокнопок (как мы делали для Modal).
Блок "Поведение" (Чекбоксы: Quick Jumper, Size Changer, Disabled и т.д.).
💻 Код: Отображение динамически генерируемого кода с кнопками копирования.
📚 Примеры: Галерея преднастроенных вариантов пагинации для быстрого копирования.
📖 API: Вставка control-documentation. 5. Стилизация (
.component.scss
)
Использование стандартных стилей demo-form, control-group и form-section-title.
Настройка nz-radio-group в одну или несколько строк для компактности.
Полная поддержка Dark Theme для всех элементов управления и блоков кода. 6. Финализация
Проверка маршрутизации (уже создана пользователем).
Проверка связки showcase-docs (дублирование документации в самом низу страницы под всеми блоками).
Тестирование адаптивности Playground на разных разрешениях.
