---
description: Создать резервную копию компонента av-image-editor-vs
---

Этот ворклоу запускает скрипт резервного копирования, который сохраняет текущее состояние редактора в папку `backups` с меткой времени.

// turbo

1. Запустить npm скрипт резервного копирования:
   `npm run backup:editor`
