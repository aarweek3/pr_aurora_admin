# @aurora/i18n для одноязычных сайтов

## 🤔 Нужна ли вам i18n библиотека для одноязычных сайтов?

**Короткий ответ: НЕТ, не нужна.**

Если каждый домен имеет только один язык:

- `mysite.com` → только английский
- `mysite.de` → только немецкий
- `mysite.fr` → только французский

То вам НЕ нужна библиотека интернационализации вообще.

## 🎯 Простое решение для одноязычных сайтов

### Вариант 1: Прямой текст в компонентах (рекомендуется)

```typescript
// mysite.com - английская версия
@Component({
  template: `
    <header>
      <h1>Welcome to My Website</h1>
      <nav>
        <a href="/about">About Us</a>
        <a href="/contact">Contact</a>
      </nav>
    </header>
  `,
})
export class HeaderComponent {}
```

```typescript
// mysite.de - немецкая версия
@Component({
  template: `
    <header>
      <h1>Willkommen auf meiner Website</h1>
      <nav>
        <a href="/about">Über uns</a>
        <a href="/contact">Kontakt</a>
      </nav>
    </header>
  `,
})
export class HeaderComponent {}
```

### Вариант 2: Константы для переиспользования

```typescript
// src/app/constants/texts.ts (английская версия)
export const TEXTS = {
  HEADER: {
    TITLE: "Welcome to My Website",
    MENU: {
      ABOUT: "About Us",
      CONTACT: "Contact",
    },
  },
  BUTTONS: {
    SAVE: "Save",
    CANCEL: "Cancel",
  },
};

// Использование
@Component({
  template: `
    <h1>{{ TEXTS.HEADER.TITLE }}</h1>
    <button>{{ TEXTS.BUTTONS.SAVE }}</button>
  `,
})
export class MyComponent {
  TEXTS = TEXTS;
}
```

### Вариант 3: Angular Environment для текстов

```typescript
// src/environments/environment.de.ts
export const environment = {
  production: true,
  texts: {
    WELCOME: "Willkommen",
    SAVE: "Speichern",
    CANCEL: "Abbrechen",
  },
};

// src/environments/environment.en.ts
export const environment = {
  production: true,
  texts: {
    WELCOME: "Welcome",
    SAVE: "Save",
    CANCEL: "Cancel",
  },
};

// Использование
@Component({
  template: `<h1>{{ texts.WELCOME }}</h1>`,
})
export class AppComponent {
  texts = environment.texts;
}
```

## 🏗️ Структура проекта для множественных одноязычных сайтов

### Архитектура 1: Один проект, разные builds

```
my-project/
├── src/
│   ├── app/
│   ├── environments/
│   │   ├── environment.en.ts
│   │   ├── environment.de.ts
│   │   └── environment.fr.ts
│   └── texts/
│       ├── en.ts
│       ├── de.ts
│       └── fr.ts
├── angular.json
└── package.json
```

**angular.json конфигурация:**

```json
{
  "projects": {
    "my-app": {
      "architect": {
        "build": {
          "configurations": {
            "en": {
              "fileReplacements": [
                {
                  "replace": "src/environments/environment.ts",
                  "with": "src/environments/environment.en.ts"
                }
              ]
            },
            "de": {
              "fileReplacements": [
                {
                  "replace": "src/environments/environment.ts",
                  "with": "src/environments/environment.de.ts"
                }
              ]
            },
            "fr": {
              "fileReplacements": [
                {
                  "replace": "src/environments/environment.ts",
                  "with": "src/environments/environment.fr.ts"
                }
              ]
            }
          }
        }
      }
    }
  }
}
```

**Build команды:**

```bash
# Английская версия
ng build --configuration=en --output-path=dist/en

# Немецкая версия
ng build --configuration=de --output-path=dist/de

# Французская версия
ng build --configuration=fr --output-path=dist/fr
```

### Архитектура 2: Отдельные проекты

```
my-websites/
├── mysite-en/     # Английский сайт
│   ├── src/
│   ├── angular.json
│   └── package.json
├── mysite-de/     # Немецкий сайт
│   ├── src/
│   ├── angular.json
│   └── package.json
├── mysite-fr/     # Французский сайт
│   ├── src/
│   ├── angular.json
│   └── package.json
└── shared/        # Общие компоненты
    ├── components/
    ├── services/
    └── utils/
```

**Преимущества:**

- ✅ Полная изоляция языков
- ✅ Разные зависимости при необходимости
- ✅ Независимые деплои
- ❌ Дублирование кода

### Архитектура 3: Nx Monorepo (для больших проектов)

```bash
# Создание Nx workspace
npx create-nx-workspace@latest my-websites --preset=angular

# Создание приложений для каждого языка
nx g @nx/angular:app mysite-en
nx g @nx/angular:app mysite-de
nx g @nx/angular:app mysite-fr

# Общая библиотека компонентов
nx g @nx/angular:lib shared-ui
```

## 🚀 Deployment стратегии

### Стратегия 1: Поддомены

```nginx
# nginx конфигурация
server {
    server_name mysite.com;
    root /var/www/mysite-en/dist;
}

server {
    server_name mysite.de;
    root /var/www/mysite-de/dist;
}

server {
    server_name mysite.fr;
    root /var/www/mysite-fr/dist;
}
```

### Стратегия 2: Один домен, разные пути

```nginx
server {
    server_name mysite.com;

    location /en {
        alias /var/www/mysite-en/dist;
        try_files $uri $uri/ /en/index.html;
    }

    location /de {
        alias /var/www/mysite-de/dist;
        try_files $uri $uri/ /de/index.html;
    }

    location /fr {
        alias /var/www/mysite-fr/dist;
        try_files $uri $uri/ /fr/index.html;
    }
}
```

### Стратегия 3: CDN с geo-routing

```javascript
// CloudFlare Workers / AWS CloudFront
const COUNTRY_TO_SITE = {
  DE: "https://mysite.de",
  FR: "https://mysite.fr",
  US: "https://mysite.com",
  GB: "https://mysite.com",
};

export default {
  async fetch(request, env) {
    const country = request.cf.country;
    const targetSite = COUNTRY_TO_SITE[country] || "https://mysite.com";

    return fetch(targetSite + request.url.pathname);
  },
};
```

## 📊 Сравнение подходов

| Критерий                | Одноязычные сайты     | @aurora/i18n           |
| ----------------------- | --------------------- | ---------------------- |
| **Сложность**           | ⭐ Очень простая      | ⭐⭐⭐ Средняя         |
| **Bundle size**         | ⭐⭐⭐ Минимальный    | ⭐⭐ +8KB              |
| **SEO**                 | ⭐⭐⭐ Идеальный      | ⭐⭐ Хороший           |
| **Переключение языков** | ❌ Нет                | ⭐⭐⭐ Да              |
| **Обслуживание**        | ⭐⭐ Несколько сайтов | ⭐⭐⭐ Один сайт       |
| **Время загрузки**      | ⭐⭐⭐ Быстрое        | ⭐⭐ Немного медленнее |

## ⚠️ Когда всё-таки стоит использовать @aurora/i18n

### Сценарий 1: Планируете добавить переключение языков

Если в будущем хотите:

- Переключатель языков на сайте
- Сохранение выбранного языка пользователем
- Смешанный контент (часть на одном языке, часть на другом)

### Сценарий 2: Контент приходит из API

```typescript
// Если переводы загружаются с сервера
@Component({
  template: `<h1>{{ pageTitle }}</h1>`,
})
export class PageComponent implements OnInit {
  pageTitle = "";

  ngOnInit() {
    // Загружаем контент с API в зависимости от домена
    const lang = this.detectLanguageFromDomain();
    this.loadContent(lang);
  }

  private detectLanguageFromDomain(): string {
    const hostname = window.location.hostname;
    if (hostname.includes(".de")) return "de";
    if (hostname.includes(".fr")) return "fr";
    return "en";
  }
}
```

В этом случае @aurora/i18n поможет с:

- Определением языка по домену
- Загрузкой контента с API
- Кешированием переводов

## 🎯 Рекомендация

**Для одноязычных сайтов используйте простой подход:**

1. **Один проект + разные builds** (если сайты похожи)
2. **Прямой текст в компонентах** (максимальная простота)
3. **Константы для переиспользования** (если много повторяющихся текстов)

**@aurora/i18n используйте только если:**

- Планируете многоязычность в будущем
- Контент загружается динамически с API
- Нужна аналитика использования языков

**Результат:** Более простой код, меньший bundle, лучшая производительность! 🚀
