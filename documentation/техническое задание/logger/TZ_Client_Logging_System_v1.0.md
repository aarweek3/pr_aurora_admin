# Обновлённое Техническое Задание (ТЗ): Система клиентского логирования Angular (Aurora Admin Panel)

/\*\*

- Интерфейсы для системы клиентского логирования (Aurora Admin Panel)
- Описание: Строгая типизация для MVP с учётом всех корректировок ТЗ.
-           Обеспечивает сериализуемость, безопасность (unknown вместо any)
-           и расширяемость (Phase 2 поля опциональны).
-           Совместимо с Angular signals и ng-zorro UI.
-
- Версия: 1.0 (MVP)
- Дата: 26 декабря 2025
  \*/

/\*\*

- Уровни логирования (фиксированный union-тип для строгой типизации).
- Иерархия: TRACE < DEBUG < INFO < WARN < ERROR < FATAL.
- В production по умолчанию minLevel = 'WARN'.
  \*/
  export type LogLevel =
  | 'TRACE' // Детальные внутренние шаги (только development)
  | 'DEBUG' // Отладочная информация (только development)
  | 'INFO' // Значимые бизнес-события
  | 'WARN' // Некритичные проблемы
  | 'ERROR' // Ошибки, требующие внимания
  | 'FATAL'; // Критические сбои приложения

/\*\*

- Основная модель записи лога. Единственный формат во всей системе.
- Сериализуемость: timestamp как number (Unix ms), data/error как unknown.
- Безопасность: все чувствительные поля санитизируются в pipeline.
- Расширяемость: Phase 2 поля (operationCorrelationId) опциональны.
  \*/
  export interface LogEntry {
  /\*\*
  - Unix timestamp в миллисекундах (Date.now()).
  - Почему number: полная сериализуемость JSON, сортировка, сравнение без часовых поясов.
  - В UI: new Date(entry.timestamp) + DatePipe для форматирования.
    \*/
    timestamp: number;

/\*_ Уровень логирования. _/
level: LogLevel;

/\*_ Основное сообщение (обязательное, string). _/
message: string;

/\*_ Источник лога (компонент/сервис, например 'UserFormComponent'). _/
context?: string;

/\*_ Логическая группа (auth, api, ui, validation). _/
category?: string;

/\*_ Дополнительные данные (JSON-safe). Тип unknown для безопасности. _/
data?: unknown;

/\*_ Контекстная информация (автоматически из Context Provider). _/
currentRoute?: string; // Текущий маршрут (без query params)
userRole?: string; // Роль пользователя (null если не авторизован)

/** Детали ошибки (если level = ERROR/FATAL). \*/
error?: {
/** Название ошибки (Error.name). _/
name: string;
/\*\* Сообщение ошибки (Error.message). _/
message: string;
/\*_ Стек вызовов (Error.stack, опционально). _/
stack?: string;
};

/\*\* Идентификатор сессии (генерируется при старте/логине).

- Используется ТОЛЬКО для группировки логов сессии, не для трассировки операций. \*/
  sessionCorrelationId: string;

/\*_ Идентификатор отдельной операции (Phase 2, опционально). _/
operationCorrelationId?: string;
}

/\*\*

- Конфигурация Log Registry (фасад для управления настройками).
- Загружается из environment.ts или forRoot().
- В production: minLevel = 'WARN' (защита от DEBUG/TRACE).
  _/
  export interface LogRegistryConfig {
  /\*\* Минимальный уровень логирования (default: 'WARN' в production). _/
  minLevel: LogLevel;
  /\*_ Максимальное количество записей в буфере (default: 3000). _/
  limitRecords: number;
  }

/\*\*

- Контекстный адаптер (легковесный, без тяжёлых зависимостей).
- Регистрируется в Logger Core. Возвращает Partial<LogEntry> для обогащения.
- Пример: RouteContextAdapter возвращает { currentRoute }.
  \*/
  export interface ContextAdapter {
  /\*\*
  - Возвращает безопасный контекст для текущего лога.
  - Не содержит чувствительных данных (токены, формы).
  - Вызывается в pipeline на этапе Enrichment.
    \*/
    getContext(): Partial<LogEntry>;
    }

/\*\*

- Интерфейс транспорта логов (плагинная модель).
- Каждый транспорт получает уже обработанный (санитизированный) LogEntry.
- ConsoleTransport: только development, без raw data/error.
  \*/
  export interface LogTransport {
  /\*\*
  - Обрабатывает лог-запись.
  - Синхронно (console) или асинхронно (backend).
  - Гарантия: entry уже прошёл pipeline (sanitized, filtered).
    \*/
    handle(entry: LogEntry): void | Promise<void>;
    }

/\*\*

- Параметры публичного метода логирования (logger.info(), logger.warn() и т.д.).
- Используется в бизнес-коде приложения.
  _/
  export interface LogOptions {
  /\*\* Источник (опционально, по умолчанию из stack trace). _/
  context?: string;
  /** Категория (auth, api и т.д.). \*/
  category?: string;
  /** Данные (unknown для безопасности, санитизируется автоматически). \*/
  data?: unknown;
  }

/\*\*

- Расширенные опции для логирования ошибок (logger.error()).
- Автоматически извлекает name/message/stack из Error.
  _/
  export interface ErrorLogOptions extends LogOptions {
  /\*\* Ошибка (Error | unknown). Автоматически парсится в LogEntry.error. _/
  error: unknown;
  }

## 0. Цели и принципы

### 🎯 Цели

- Обеспечить централизованное клиентское логирование значимых событий приложения.
- Предоставить инструмент просмотра логов в реальном времени с фильтрацией и базовым управлением.
- Гарантировать безопасность данных путём предотвращения утечек чувствительной информации.
- Интегрировать систему с Global Status Bar и Right Panel админ-панели.
- Подготовить архитектуру к будущей отправке логов на сервер.

### 🧠 Принципы

- Logger Core — независимое ядро, полностью отделённое от UI.
- UI — исключительно потребитель логов и конфигурации.
- Запрет прямого использования console.log в бизнес-логике.
- Все логи — структурированные объекты.
- Безопасность превыше удобства: обязательная базовая санитизация.
- Реализация на Angular 17+ с standalone-компонентами и signals.
- Частичное использование ng-zorro (nz-table с виртуальным скроллом, nz-button и т.д.).
- MVP-фокус: минимально жизнеспособная версия с итеративным расширением.

## 1️⃣ Этап 1 (MVP) — Logger Core (ядро логирования)

### 1.1. Назначение

Единая точка входа для всех логов приложения.

### 1.2. Ответственность

- Приём структурированных событий.
- Обогащение контекстом.
- Базовая санитизация.
- Фильтрация по уровню.
- Хранение в памяти.
- Реактивное уведомление подписчиков (signal<LogEntry[]>).

### 1.3. Уровни логирования

| Уровень | Назначение                  |
| ------- | --------------------------- |
| TRACE   | Детальные внутренние шаги   |
| DEBUG   | Отладочная информация       |
| INFO    | Значимые бизнес-события     |
| WARN    | Некритичные проблемы        |
| ERROR   | Ошибки, требующие внимания  |
| FATAL   | Критические сбои приложения |

### 1.4. Модель LogEntry

Обязательные поля:

- timestamp (Date)
- level (LogLevel)
- message (string)

Дополнительные поля (MVP):

- context (string) — источник
- category (string) — группа (auth, api, ui и т.д.)
- data (any, JSON-safe)
- error (object) — { name, message, stack }
- sessionCorrelationId (string) — идентификатор сессии

### 1.5. Внутренняя структура ядра (логическое разделение)

- EntryBuilder — сбор и enrichment.
- PipelineProcessor — последовательная обработка (sanitization, filtering).
- StorageManager — управление буфером.
- TransportDispatcher — рассылка.

### 1.6. Внутренний pipeline

Последовательная обработка:

1. RawEntry → Enrichment (контекст)
2. → Sanitization (базовая)
3. → Filtering (по уровню)
4. → Storage
5. → Dispatch to Transports

## 2️⃣ Этап 2 (MVP) — Context Provider

### 2.1. Назначение

Автоматическое обогащение логов безопасным контекстом.

### 2.2. Содержимое контекста (MVP)

- currentRoute (string)
- userRole (string | null)
- sessionCorrelationId (string, генерируется при старте сессии)

**Уточнение**: SessionCorrelationId используется исключительно для группировки логов одной пользовательской сессии и не предназначен для трассировки отдельных операций или запросов.

**Phase 2**: operationCorrelationId для отдельных операций.

### 2.3. Реализация

- Легковесные adapters (RouteContextAdapter, AuthContextAdapter).
- Регистрация adapters при конфигурации.
- Нет прямого инжектирования тяжёлых сервисов.

## 3️⃣ Этап 3 (MVP) — Log Registry & Security

### 3.1. Log Registry (фасад конфигурации)

- Отдельный сервис для управления настройками.
- Параметры: minLevel (default WARN в production), limitRecords (default 3000).

### 3.2. Безопасность (MVP)

- Простой blacklist ключей (password, token, accessToken, refreshToken и т.д.).
- Маскировка значения (\*\*\*\*) для совпадающих ключей.
- Санитизация в pipeline до хранения.

**Phase 2**: Глубокий обход, hashing, partial reveal.

## 4️⃣ Этап 4 (MVP) — Storage & Transport

### 4.1. Storage

- In-memory circular buffer (FIFO) — единственный источник истины.
- Конфигурируемый лимит с авто-очисткой.

### 4.2. Transport

- Интерфейс LogTransport { handle(entry: LogEntry): void }
- Встроенные (MVP):
  - ConsoleTransport (только development; использует исключительно санитизированные LogEntry; запрещён вывод raw data/error).

**Phase 2**: BackendTransport, IndexedDBTransport.

## 5️⃣ Этап 5 (MVP) — Error Handling Integration

- Global ErrorHandler: перехват uncaught errors → ERROR-лог с контекстом.
- Генерация sessionCorrelationId при старте приложения.

**Phase 2**: HttpInterceptor для логирования запросов/ответов.

## 6️⃣ Этап 6 (MVP) — Logger UI (Log Viewer)

### 6.1. Размещение

- Отдельная страница /dev/logs или Right Panel.

### 6.2. Функции (MVP)

- nz-table с виртуальным скроллом.
- Колонки: Timestamp, Level (цвет), Category, Context, Message.
- Expand row для data/stack.
- Фильтры: уровень, категория, поиск по тексту (debounce).
- Сортировка по времени.
- Авто-скролл (toggle).
- Clear logs.
- Export JSON/CSV (только по текущей выборке с применёнными фильтрами; содержит исключительно санитизированные записи; в production — отключён или с предупреждением: «Экспорт содержит отладочную информацию. Используйте осторожно»).

**Phase 2**: Расширенные фильтры, диапазон дат.

## 7️⃣ Этап 7 (Phase 2) — Управление из UI (Control Panel)

- UI взаимодействует только с LogRegistry.
- Защита: в production уровни ниже WARN недоступны для изменения.

## 8️⃣ Этап 8 (MVP) — Интеграция со Status Bar

- Индикатор количества ERROR/WARN.
- Иконка при наличии ERROR/FATAL.
- Клик → открытие Log Viewer.

## 9️⃣ Этап 9 — Производительность (обязательно в MVP)

- Лимит записей + виртуальный скролл.
- Debounce фильтров.
- Batch-обновления сигнала.

## 🔟 Этап 10 (Phase 2+) — Расширяемость

- Плагинная модель transport’ов и adapters.
- Logger Core как независимый пакет.
- Подготовка отдельного документа "Public API Contract" после MVP.
- Введение Error Policy для унификации классификации событий.

## 🧩 Финальная архитектурная схема (MVP)

```
Application
 └── Logger Core
      ├── Context Adapters
      ├── Log Registry (config)
      ├── Pipeline (логическое разделение)
      └── Storage (единственный источник истины, signal<LogEntry[]>)
           ↓
      Log Viewer UI
           ↓
      Global Status Bar
           ↓
      ErrorHandler
```

Данное ТЗ является **окончательной обновлённой версией** с интеграцией всех рекомендаций и корректировок. Система обеспечивает максимальную безопасность, производительность и готовность к расширению, сохраняя фокус на MVP.
