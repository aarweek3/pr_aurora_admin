Выполни полный рефакторинг модуля `[ПУТЬ_К_ВАШЕМУ_КОМПОНЕНТУ]` (например, src\app\AGREGATOR\PAGES\SPRAVKA\NewComponentPage), приведя его к «Золотому стандарту» Aurora v3.5.

**В качестве эталона архитектуры, верстки и логики используй:** `src\app\AGREGATOR\PAGES\SPRAVKA\DeveloperOfAggregatorPage`

**Необходимые изменения по слоям:**

1. **Manager** : Настрой обертку модального окна (ширина 1000px), вынеси кнопки «Сохранить/Отмена» в футер через `@ViewChild` формы, как в эталоне.
2. **Form** : Переведи на Grid-сетку (`nz-row`/`nz-col`). Интегрируй `SeoFormComponent` внутрь каждой языковой вкладки. Реализуй универсальный загрузчик иконок/изображений через `AvUniversalUploadModalComponent` и `ImageServiceUniversal`.
3. **List** : Оберни в `nz-card`, добавь `trash-alert` для режима корзины, настрой серверную сортировку в заголовках таблицы и замени стандартную пагинацию на `av-pagination`.
4. **State Service** : Используй паттерн `executeWithLoading`. Добавь стандартные методы-хелперы для пагинации, сортировки и фильтрации (`setSort`, `setPageIndex`, `setSearch` и т.д.).
5. **View Modal** : Переверстай на `nz-descriptions` и используй вкладки с `nz-collapse` для отображения SEO-данных.

**Важно:** Сохраняй специфичные поля данных нового компонента, но упакуй их в визуальный стиль и программную логику «Золотого образца».
